# Git Tab Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 mux0 右上角加一个 git 图标按钮，点击后在当前 workspace 创建（或切换到）一个 "Git" tab，自动启动可配置的 git viewer 命令（默认 `lazygit`）。所有 tab pill 加 leading SF Symbol 图标做身份区分。

**Architecture:** 给 `TerminalTab` 加 `kind: TabKind?` 字段；`WorkspaceStore.ensureGitTab(in:)` 处理"找到就 select、没找到就创建"的复用语义；`TabContentView.resolvedStartupCommand` 在 kind=.git 时注入 `mux0-git-viewer` setting 的命令；`TabBarView.TabItemView` 增加 leading `NSImageView`；`ContentView` ZStack 增加对称于 sidebar toggle 的 `IconButton`。

**Tech Stack:** Swift 5 / AppKit / SwiftUI / XCTest / xcstrings；macOS 14+；SF Symbols `arrow.triangle.branch`、`terminal`。

**Spec:** [`docs/superpowers/specs/2026-04-30-git-tab-design.md`](../specs/2026-04-30-git-tab-design.md)

---

## Files

| 路径 | 动作 | 职责 |
|------|------|------|
| `mux0/Models/Workspace.swift` | Modify | 新增 `TabKind` enum；TerminalTab 加可选 `kind` 字段 |
| `mux0/Models/WorkspaceStore.swift` | Modify | `addTab` 加可选 kind 参数；新增 `ensureGitTab(in:)` |
| `mux0Tests/WorkspaceStoreTests.swift` | Modify | `TabKind` Codable 测试（新值 / nil / 旧 JSON 兼容）+ `ensureGitTab` 测试 |
| `mux0/Localization/Localizable.xcstrings` | Modify | 新增 5 个 i18n key（en + zh-Hans）|
| `mux0/Localization/L10n.swift` | Modify | 加 `Topbar` namespace、扩 `Tab` / `Settings.Shell` |
| `mux0Tests/L10nSmokeTests.swift` | Modify | `allKeys` 数组追加新 key |
| `mux0/Settings/Sections/ShellSectionView.swift` | Modify | 加 `mux0-git-viewer` 的 `BoundTextField` |
| `mux0/TabContent/TabContentView.swift` | Modify | `resolvedStartupCommand` 加 git tab 分支 |
| `mux0/TabContent/TabBarView.swift` | Modify | `TabItemView` 加 leading `NSImageView`（kind 感知）|
| `mux0/ContentView.swift` | Modify | ZStack overlay 加右上角 git `IconButton` |
| `docs/settings-reference.md` | Modify | 追加 `mux0-git-viewer` 字段说明 |
| `docs/architecture.md` | Modify | Tab content 章节追加 git tab 行为说明 |
| `CLAUDE.md` | Modify | Common Tasks 表新增一行；Models/Workspace.swift 注释提及 TabKind |

---

## Task 1: 数据模型 —— `TabKind` enum + `TerminalTab.kind` 字段

**Files:**
- Modify: `mux0/Models/Workspace.swift`
- Modify: `mux0Tests/WorkspaceStoreTests.swift`

`TerminalTab` 当前用合成 Codable（直接 `: Codable`，没有自定义 init/encode）。Swift 合成 decoder 对 `Optional` 类型自动用 `decodeIfPresent`，**默认值** `= nil` 仅作用于成员 init —— 但因为类型本身就是 Optional，缺失键会落到 nil，向后兼容自动满足。

- [ ] **Step 1: 写失败的测试 —— TabKind 编码包含 git 值**

在 `mux0Tests/WorkspaceStoreTests.swift` 文件末尾（`}` 之前）追加：

```swift
    // MARK: - TabKind & ensureGitTab

    func testTerminalTabKind_codableRoundTrip_gitValue() throws {
        var tab = TerminalTab(title: "Git")
        tab.kind = .git

        let data = try JSONEncoder().encode(tab)
        let decoded = try JSONDecoder().decode(TerminalTab.self, from: data)

        XCTAssertEqual(decoded.kind, .git)
        XCTAssertEqual(decoded.title, "Git")
    }

    func testTerminalTabKind_codableRoundTrip_nilValue() throws {
        let tab = TerminalTab(title: "terminal 1")
        XCTAssertNil(tab.kind)

        let data = try JSONEncoder().encode(tab)
        let decoded = try JSONDecoder().decode(TerminalTab.self, from: data)

        XCTAssertNil(decoded.kind)
    }

    func testTerminalTabKind_decodingLegacyJSONWithoutKindField() throws {
        // Old persistence (pre-TabKind) had no `kind` key. Decoding must succeed
        // and land on nil, otherwise existing UserDefaults blobs break on upgrade.
        let termId = UUID()
        let tabId = UUID()
        let json = """
        {
            "id": "\(tabId.uuidString)",
            "title": "old tab",
            "layout": { "type": "terminal", "terminalId": "\(termId.uuidString)" },
            "focusedTerminalId": "\(termId.uuidString)"
        }
        """
        let data = json.data(using: .utf8)!

        let decoded = try JSONDecoder().decode(TerminalTab.self, from: data)

        XCTAssertNil(decoded.kind)
        XCTAssertEqual(decoded.title, "old tab")
        XCTAssertEqual(decoded.id, tabId)
    }
```

- [ ] **Step 2: 跑测试，确认失败**

```bash
xcodebuild test -project mux0.xcodeproj -scheme mux0Tests \
    -only-testing:mux0Tests/WorkspaceStoreTests/testTerminalTabKind_codableRoundTrip_gitValue \
    -only-testing:mux0Tests/WorkspaceStoreTests/testTerminalTabKind_codableRoundTrip_nilValue \
    -only-testing:mux0Tests/WorkspaceStoreTests/testTerminalTabKind_decodingLegacyJSONWithoutKindField
```

期望：编译失败（`TerminalTab` 没有 `kind` 字段、`TabKind` 类型不存在）。

- [ ] **Step 3: 在 Workspace.swift 中实现 TabKind + kind 字段**

打开 `mux0/Models/Workspace.swift`，在 `// MARK: - TerminalTab` 上方插入：

```swift
// MARK: - TabKind

/// 标记 tab 的语义类型。nil 表示普通终端 tab。
/// 当前唯一的非 nil 取值是 `.git` —— 由右上角 git 图标创建，渲染时
/// `TabContentView.resolvedStartupCommand` 会把 `mux0-git-viewer` 设置中
/// 的命令作为 initial_input 注入到该 tab 的首终端。
enum TabKind: String, Codable, Equatable {
    case git
}
```

修改 `TerminalTab` 结构体（在 `var focusedTerminalId: UUID` 后面加一行 `var kind`）：

```swift
struct TerminalTab: Codable, Identifiable, Equatable {
    let id: UUID
    var title: String
    var layout: SplitNode
    var focusedTerminalId: UUID
    var kind: TabKind? = nil

    init(id: UUID = UUID(), title: String, terminalId: UUID = UUID(), kind: TabKind? = nil) {
        self.id = id
        self.title = title
        self.layout = .terminal(terminalId)
        self.focusedTerminalId = terminalId
        self.kind = kind
    }
}
```

> **注意**：这里不需要写自定义 `init(from:)` / `encode(to:)`。Swift 合成 Codable 对 `Optional` 字段自动用 `decodeIfPresent` —— 旧 JSON 无 `kind` 键时落到 nil，兼容性靠类型本身（不靠默认值）保证。这一点由 Step 1 的 legacy JSON 测试覆盖。

- [ ] **Step 4: 跑测试，确认通过**

```bash
xcodebuild test -project mux0.xcodeproj -scheme mux0Tests \
    -only-testing:mux0Tests/WorkspaceStoreTests/testTerminalTabKind_codableRoundTrip_gitValue \
    -only-testing:mux0Tests/WorkspaceStoreTests/testTerminalTabKind_codableRoundTrip_nilValue \
    -only-testing:mux0Tests/WorkspaceStoreTests/testTerminalTabKind_decodingLegacyJSONWithoutKindField
```

期望：3 个 case 全部 PASS。

- [ ] **Step 5: 跑全量 WorkspaceStoreTests（确认未破坏现有测试）**

```bash
xcodebuild test -project mux0.xcodeproj -scheme mux0Tests \
    -only-testing:mux0Tests/WorkspaceStoreTests
```

期望：所有 case PASS。

- [ ] **Step 6: 提交**

```bash
git add mux0/Models/Workspace.swift mux0Tests/WorkspaceStoreTests.swift
git commit -m "$(cat <<'EOF'
feat(models): add TabKind enum + TerminalTab.kind field

Lays groundwork for kind-aware tab semantics. Codable round-trips
both kind=.git and kind=nil; legacy JSON without the field decodes
to kind=nil for backward compat with persisted UserDefaults blobs.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: `WorkspaceStore.addTab(to:kind:)` + `ensureGitTab(in:)`

**Files:**
- Modify: `mux0/Models/WorkspaceStore.swift`
- Modify: `mux0Tests/WorkspaceStoreTests.swift`

按 spec：`addTab` 透传 kind；`ensureGitTab` 处理"已有就切、没有就建"，并回传切换前的 focused terminalId 用于 pwd 继承。

- [ ] **Step 1: 写失败的测试 —— ensureGitTab 三种场景**

在 `mux0Tests/WorkspaceStoreTests.swift` 的 Task 1 测试之后追加：

```swift
    func testEnsureGitTab_createsNewWhenAbsent() {
        let store = WorkspaceStore(persistenceKey: "test-\(UUID())")
        store.createWorkspace(name: "ws")
        let wsId = store.workspaces[0].id
        let tabsBefore = store.workspaces[0].tabs.count

        let result = store.ensureGitTab(in: wsId)

        XCTAssertTrue(result.isNew)
        XCTAssertEqual(store.workspaces[0].tabs.count, tabsBefore + 1)
        XCTAssertEqual(store.workspaces[0].selectedTabId, result.tabId)
        let newTab = store.workspaces[0].tabs.first(where: { $0.id == result.tabId })
        XCTAssertEqual(newTab?.kind, .git)
        XCTAssertEqual(newTab?.title, "Git")
        XCTAssertEqual(newTab?.layout.allTerminalIds().first, result.terminalId)
    }

    func testEnsureGitTab_reusesWhenPresent() {
        let store = WorkspaceStore(persistenceKey: "test-\(UUID())")
        store.createWorkspace(name: "ws")
        let wsId = store.workspaces[0].id
        let firstResult = store.ensureGitTab(in: wsId)
        let tabsAfterFirst = store.workspaces[0].tabs.count

        // Switch focus away from the git tab, then re-invoke
        let originalTabId = store.workspaces[0].tabs[0].id
        store.selectTab(id: originalTabId, in: wsId)

        let secondResult = store.ensureGitTab(in: wsId)

        XCTAssertFalse(secondResult.isNew)
        XCTAssertEqual(secondResult.tabId, firstResult.tabId)
        XCTAssertEqual(store.workspaces[0].tabs.count, tabsAfterFirst)  // no new tab created
        XCTAssertEqual(store.workspaces[0].selectedTabId, firstResult.tabId)  // selection moved back
    }

    func testEnsureGitTab_returnsSourcePwdTerminalIdFromPreviouslyFocusedTab() {
        let store = WorkspaceStore(persistenceKey: "test-\(UUID())")
        store.createWorkspace(name: "ws")
        let wsId = store.workspaces[0].id
        // The auto-created tab from createWorkspace has one terminal — capture its id.
        let originalTab = store.workspaces[0].tabs[0]
        let originalTermId = originalTab.layout.allTerminalIds()[0]
        XCTAssertEqual(store.workspaces[0].selectedTabId, originalTab.id)

        let result = store.ensureGitTab(in: wsId)

        // Source = focused terminal of the tab that was selected BEFORE we switched.
        XCTAssertEqual(result.sourcePwdTerminalId, originalTermId)
    }

    func testEnsureGitTab_reuseScenarioStillReportsSourcePwdTerminalId() {
        let store = WorkspaceStore(persistenceKey: "test-\(UUID())")
        store.createWorkspace(name: "ws")
        let wsId = store.workspaces[0].id
        _ = store.ensureGitTab(in: wsId)  // create
        let nonGit = store.workspaces[0].tabs.first(where: { $0.kind == nil })!
        store.selectTab(id: nonGit.id, in: wsId)
        let nonGitTermId = nonGit.layout.allTerminalIds()[0]

        let result = store.ensureGitTab(in: wsId)

        XCTAssertFalse(result.isNew)
        XCTAssertEqual(result.sourcePwdTerminalId, nonGitTermId)
    }

    func testEnsureGitTab_unknownWorkspaceReturnsNoOp() {
        let store = WorkspaceStore(persistenceKey: "test-\(UUID())")
        store.createWorkspace(name: "ws")

        // Calling on an unknown workspace must not crash and must not mutate anything.
        let tabsBefore = store.workspaces[0].tabs.count
        _ = store.ensureGitTab(in: UUID())

        XCTAssertEqual(store.workspaces[0].tabs.count, tabsBefore)
    }
```

- [ ] **Step 2: 跑测试，确认失败**

```bash
xcodebuild test -project mux0.xcodeproj -scheme mux0Tests \
    -only-testing:mux0Tests/WorkspaceStoreTests/testEnsureGitTab_createsNewWhenAbsent
```

期望：编译失败（`ensureGitTab` 不存在）。

- [ ] **Step 3: 实现 `addTab(to:kind:)` 签名变更**

打开 `mux0/Models/WorkspaceStore.swift`，把现有 `addTab(to:)` 改成：

```swift
    @discardableResult
    func addTab(to workspaceId: UUID, kind: TabKind? = nil) -> (tabId: UUID, terminalId: UUID)? {
        guard let wsIdx = wsIndex(workspaceId) else { return nil }
        let index = workspaces[wsIdx].tabs.count + 1
        let title: String
        switch kind {
        case .git:  title = "Git"
        case .none: title = "terminal \(index)"
        }
        var tab = makeNewTab(index: index)
        tab.title = title
        tab.kind = kind
        workspaces[wsIdx].tabs.append(tab)
        workspaces[wsIdx].selectedTabId = tab.id
        save()
        return (tabId: tab.id, terminalId: tab.layout.allTerminalIds()[0])
    }
```

> 默认实参 `kind: TabKind? = nil` 保证现有所有调用点（`TabContentView.addNewTab`、`mux0NewTab` notification handler 等）行为不变。

- [ ] **Step 4: 实现 `ensureGitTab(in:)`**

在 WorkspaceStore.swift 的 `// MARK: - Tab CRUD` 区块末尾（`renameTab` 函数之后）追加：

```swift
    /// 保证给定 workspace 至少有一个 `kind == .git` 的 tab，并切到它。
    ///
    /// - 如果已经存在：仅 `selectTab`，不改 tabs 数组。
    /// - 如果不存在：调 `addTab(to:kind: .git)` 创建并切。
    ///
    /// `sourcePwdTerminalId` = 调用此方法 *之前* selected tab 的 focusedTerminalId，
    /// 由调用方用于 `TerminalPwdStore.inherit(from:to:)`，让 lazygit 落地在
    /// 用户当下浏览的仓库 pwd。必须在 `selectTab` 切换之前 capture，否则之后
    /// 取到的就是新 git tab 自身的终端，pwd 继承变成自我赋值。
    @discardableResult
    func ensureGitTab(in workspaceId: UUID) -> (
        tabId: UUID,
        terminalId: UUID,
        isNew: Bool,
        sourcePwdTerminalId: UUID?
    ) {
        guard let wsIdx = wsIndex(workspaceId) else {
            return (UUID(), UUID(), false, nil)
        }
        // Capture before any mutation.
        let sourcePwdTerminalId: UUID? = {
            guard let selId = workspaces[wsIdx].selectedTabId,
                  let selTab = workspaces[wsIdx].tabs.first(where: { $0.id == selId })
            else { return nil }
            return selTab.focusedTerminalId
        }()

        if let existing = workspaces[wsIdx].tabs.first(where: { $0.kind == .git }) {
            selectTab(id: existing.id, in: workspaceId)
            return (existing.id, existing.focusedTerminalId, false, sourcePwdTerminalId)
        }

        guard let created = addTab(to: workspaceId, kind: .git) else {
            return (UUID(), UUID(), false, sourcePwdTerminalId)
        }
        return (created.tabId, created.terminalId, true, sourcePwdTerminalId)
    }
```

> `wsIndex(workspaceId)` 返回 nil 时（workspace id 找不到）必须早返回——返回值里的两个 UUID 不会被调用方使用（因为 isNew=false 且没有真实 tab），但占位 UUID 比强解包更安全；`testEnsureGitTab_unknownWorkspaceReturnsNoOp` 覆盖这条路径。

- [ ] **Step 5: 跑测试，确认通过**

```bash
xcodebuild test -project mux0.xcodeproj -scheme mux0Tests \
    -only-testing:mux0Tests/WorkspaceStoreTests
```

期望：全部 PASS（包括 5 个新增 + 现有所有 case）。

- [ ] **Step 6: 提交**

```bash
git add mux0/Models/WorkspaceStore.swift mux0Tests/WorkspaceStoreTests.swift
git commit -m "$(cat <<'EOF'
feat(models): add WorkspaceStore.ensureGitTab + addTab kind param

ensureGitTab handles the "find or create" semantic for the git tab
within a workspace. sourcePwdTerminalId captures the previously
focused terminal so the caller can inherit pwd into the new git pane.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: i18n keys

**Files:**
- Modify: `mux0/Localization/Localizable.xcstrings`
- Modify: `mux0/Localization/L10n.swift`
- Modify: `mux0Tests/L10nSmokeTests.swift`

新增 5 个 key。L10nSmokeTests 的 `allKeys` 是 hardcoded list，必须同步追加。

- [ ] **Step 1: 写失败的测试 —— allKeys 多 5 项**

打开 `mux0Tests/L10nSmokeTests.swift`，在 `allKeys` 数组末尾（在 `"tab.row.rename",` 之后）追加：

```swift
        "settings.shell.gitViewer.help",
        "settings.shell.gitViewer.installHint",
        "settings.shell.gitViewer.label",
        "tab.git.defaultTitle",
        "topbar.gitButton.tooltip",
```

- [ ] **Step 2: 跑测试，确认失败**

```bash
xcodebuild test -project mux0.xcodeproj -scheme mux0Tests \
    -only-testing:mux0Tests/L10nSmokeTests
```

期望：`testAllKeysResolveInEnBundle` / `testAllKeysResolveInZhHansBundle` 失败，说每个新 key 缺翻译。

- [ ] **Step 3: 在 Localizable.xcstrings 加 5 个 key**

打开 `mux0/Localization/Localizable.xcstrings`，在 `"strings"` 顶层对象里（按 key 字母序）插入以下 5 个条目。每个条目格式与既有条目一致（`extractionState` 用 `"manual"`）：

```json
    "settings.shell.gitViewer.help" : {
      "extractionState" : "manual",
      "localizations" : {
        "en" : { "stringUnit" : { "state" : "translated", "value" : "Runs in the Git tab created by clicking the Git icon in the top-right. Try `lazygit`, `gitui`, or `tig`." } },
        "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "点击右上角 Git 图标后，会在新建的 Git tab 里自动执行该命令。可选：lazygit、gitui、tig。" } }
      }
    },
    "settings.shell.gitViewer.installHint" : {
      "extractionState" : "manual",
      "localizations" : {
        "en" : { "stringUnit" : { "state" : "translated", "value" : "Install lazygit: brew install lazygit" } },
        "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "安装 lazygit：brew install lazygit" } }
      }
    },
    "settings.shell.gitViewer.label" : {
      "extractionState" : "manual",
      "localizations" : {
        "en" : { "stringUnit" : { "state" : "translated", "value" : "Git viewer command" } },
        "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "Git 工具命令" } }
      }
    },
    "tab.git.defaultTitle" : {
      "extractionState" : "manual",
      "localizations" : {
        "en" : { "stringUnit" : { "state" : "translated", "value" : "Git" } },
        "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "Git" } }
      }
    },
    "topbar.gitButton.tooltip" : {
      "extractionState" : "manual",
      "localizations" : {
        "en" : { "stringUnit" : { "state" : "translated", "value" : "Open Git Viewer" } },
        "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "打开 Git 视图" } }
      }
    },
```

> 把它们插到正确的字母位置（xcstrings 是 JSON object，但 Xcode 会按 key 字母序展示）。如果用纯文本编辑器追加，确认前一个条目末尾有逗号，最后一个 entry 后没有多余逗号。

- [ ] **Step 4: 在 L10n.swift 加常量**

打开 `mux0/Localization/L10n.swift`：

(a) 把 `enum Tab` 块改成：

```swift
    enum Tab {
        static let newTabTooltip        = LocalizedStringResource("tab.newTab")
        static let gitDefaultTitle      = LocalizedStringResource("tab.git.defaultTitle")
        // Row rename/close (context menu) and close-tab alert strings are resolved
        // at runtime via L10n.string("tab.row.rename") etc. — they live only in
        // AppKit call sites (NSMenuItem, NSAlert), so no typed constant is needed.
        // See Strings.xcstrings for the full key list.
    }
```

(b) 把 `enum Shell` 块（在 `enum Settings` 内）改成：

```swift
        enum Shell {
            static let integration         = LocalizedStringResource("settings.shell.integration")
            static let features            = LocalizedStringResource("settings.shell.features")
            static let customCommand       = LocalizedStringResource("settings.shell.customCommand")
            static let defaultPlaceholder  = LocalizedStringResource("settings.shell.defaultPlaceholder")
            static let gitViewerLabel      = LocalizedStringResource("settings.shell.gitViewer.label")
            static let gitViewerHelp       = LocalizedStringResource("settings.shell.gitViewer.help")
            static let gitViewerInstallHint = LocalizedStringResource("settings.shell.gitViewer.installHint")
        }
```

(c) 在 `// MARK: - Menu` 之上插入：

```swift
    // MARK: - Topbar (overlay buttons in ContentView's title bar band)

    enum Topbar {
        static let gitButtonTooltip     = LocalizedStringResource("topbar.gitButton.tooltip")
    }
```

- [ ] **Step 5: 跑测试，确认通过**

```bash
xcodebuild test -project mux0.xcodeproj -scheme mux0Tests \
    -only-testing:mux0Tests/L10nSmokeTests
```

期望：`testAllKeysResolveInEnBundle` / `testAllKeysResolveInZhHansBundle` 全部 PASS。

- [ ] **Step 6: 提交**

```bash
git add mux0/Localization/Localizable.xcstrings mux0/Localization/L10n.swift mux0Tests/L10nSmokeTests.swift
git commit -m "$(cat <<'EOF'
feat(i18n): add 5 keys for git tab feature

tab.git.defaultTitle, topbar.gitButton.tooltip, and three
settings.shell.gitViewer.* keys (label/help/installHint).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: Settings —— `mux0-git-viewer` 字段

**Files:**
- Modify: `mux0/Settings/Sections/ShellSectionView.swift`
- Modify: `docs/settings-reference.md`

`BoundTextField` 自动把"清空"映射到 `settings.set(key, nil)`，所以 `settingsStore.get("mux0-git-viewer")` 返回 nil 时 `resolvedStartupCommand` 落 lazygit 默认（Task 5 会写）。

- [ ] **Step 1: 改 ShellSectionView**

把 `mux0/Settings/Sections/ShellSectionView.swift` 整体替换为：

```swift
import SwiftUI

struct ShellSectionView: View {
    let theme: AppTheme
    let settings: SettingsConfigStore

    private static let managedKeys = [
        "shell-integration",
        "shell-integration-features",
        "command",
        "mux0-git-viewer",
    ]

    var body: some View {
        Form {
            BoundSegmented(
                settings: settings,
                key: "shell-integration",
                options: ["detect", "none", "fish", "zsh", "bash"],
                label: L10n.Settings.Shell.integration
            )

            BoundMultiSelect(
                settings: settings,
                key: "shell-integration-features",
                allOptions: ["cursor", "sudo", "title", "ssh-env"],
                label: L10n.Settings.Shell.features
            )

            BoundTextField(
                settings: settings,
                theme: theme,
                key: "command",
                placeholder: L10n.Settings.Shell.defaultPlaceholder,
                label: L10n.Settings.Shell.customCommand
            )

            Section {
                BoundTextField(
                    settings: settings,
                    theme: theme,
                    key: "mux0-git-viewer",
                    placeholder: LocalizedStringResource("lazygit"),
                    label: L10n.Settings.Shell.gitViewerLabel
                )
            } footer: {
                VStack(alignment: .leading, spacing: 2) {
                    Text(L10n.Settings.Shell.gitViewerHelp)
                    Text(L10n.Settings.Shell.gitViewerInstallHint)
                        .foregroundStyle(.secondary)
                }
                .font(.footnote)
            }

            SettingsResetRow(settings: settings, keys: Self.managedKeys)
        }
        .formStyle(.grouped)
        .scrollContentBackground(.hidden)
    }
}
```

> 关键：把 `mux0-git-viewer` 单独包在 `Section { } footer:` 里，是为了让 help + install hint 直接挂在该字段下方（与 Form `.grouped` 的视觉分组语义一致）。其他字段共享一个隐式 Section。`placeholder` 用纯字面 `"lazygit"`，不进 xcstrings —— 它是命令名，不需要翻译。

- [ ] **Step 2: 编译并目视验证**

```bash
xcodebuild -project mux0.xcodeproj -scheme mux0 -configuration Debug build
```

期望：build SUCCESS。手动验证（用户已在跑 mux0，不主动重启）：本步只确保编译过；UI 由用户下次启动时观察。

- [ ] **Step 3: 更新 settings-reference 文档**

打开 `docs/settings-reference.md`，找到合适的 mux0-* 字段表（如果没有 mux0 自定义字段表，看文档结构选最贴近 Shell 类的位置）追加：

```markdown
### `mux0-git-viewer`

点击右上角 Git 图标后，新创建的 Git tab 会把这条命令作为 `initial_input` 注入到首终端，由 ghostty surface 启动后自动执行。

- **类型**：字符串
- **默认值**：`lazygit`（设置为空即视为默认）
- **范围**：mux0 私有字段，不传给 ghostty
- **典型值**：`lazygit`、`gitui`、`tig`、`git log --oneline --graph --all`
```

> 确切的小节标题层级以现有文档为准；如果文档已有 "Mux0-Specific Settings" 之类的章节，直接追加到末尾。

- [ ] **Step 4: 提交**

```bash
git add mux0/Settings/Sections/ShellSectionView.swift docs/settings-reference.md
git commit -m "$(cat <<'EOF'
feat(settings): add mux0-git-viewer field in Shell section

Configures the command auto-run in the Git tab. Empty falls back to
lazygit at consumption time (TabContentView.resolvedStartupCommand).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: 命令注入 —— `TabContentView.resolvedStartupCommand`

**Files:**
- Modify: `mux0/TabContent/TabContentView.swift`

按 spec：仅对"该 tab 首终端 + tab.kind == .git"注入 git viewer 命令；不影响 split 出的次级 pane；与现有 agent prefill / defaultCommand 逻辑互斥（git 优先，因为是显式语义）。

- [ ] **Step 1: 修改 `resolvedStartupCommand`**

打开 `mux0/TabContent/TabContentView.swift`，定位到 `private func resolvedStartupCommand(forTerminal id: UUID) -> String?`（约 252 行附近），把整个方法替换成：

```swift
    /// Pick the shell command to auto-execute on a fresh surface. Source order:
    ///   0. Git tab's first terminal: the configured `mux0-git-viewer` command
    ///      (default "lazygit"). Fires every time the surface is built — so
    ///      restarting mux0 re-runs the viewer automatically. Splitting inside
    ///      a git tab does NOT apply this to the new pane (only the layout's
    ///      first terminal id matches).
    ///   1. Pending agent resume command (`claude --resume <id>` /
    ///      `codex resume <id>`) — only when the matching agent's Resume
    ///      toggle in Settings is ON. Default OFF means stale UserDefaults
    ///      entries from earlier runs (or before the toggle existed) are
    ///      ignored, not replayed.
    ///   2. Workspace-level `defaultCommand`.
    private func resolvedStartupCommand(forTerminal id: UUID) -> String? {
        // (0) Git tab's first terminal → mux0-git-viewer setting.
        if let tab = store?.selectedWorkspace?.tabs.first(where: {
                $0.layout.allTerminalIds().contains(id)
            }),
            tab.kind == .git,
            id == tab.layout.allTerminalIds().first {
            let raw = settingsStore?.get("mux0-git-viewer")?
                .trimmingCharacters(in: .whitespacesAndNewlines)
            let cmd = (raw?.isEmpty == false) ? raw! : "lazygit"
            return "\(cmd)\n"
        }

        // (1) Agent resume.
        if let pending = store?.consumePendingPrefill(terminalId: id),
           let agent = HookMessage.Agent.fromResumeCommand(pending),
           settingsStore?.get(agent.resumeSettingsKey) == "true" {
            return pending
        }

        // (2) Workspace default command.
        return store?.selectedWorkspace?.defaultCommand
    }
```

- [ ] **Step 2: 编译验证**

```bash
xcodebuild -project mux0.xcodeproj -scheme mux0 -configuration Debug build
```

期望：build SUCCESS。

- [ ] **Step 3: 跑测试（确认未误伤现有逻辑）**

```bash
xcodebuild test -project mux0.xcodeproj -scheme mux0Tests
```

期望：所有 case PASS。

> 不为这条路径写单元测试：`resolvedStartupCommand` 是 private + 依赖 store/settings/ghostty surface 的多边构造，单元测试需要 mock 一整个图。改用 Task 8 的手动验证清单覆盖。

- [ ] **Step 4: 提交**

```bash
git add mux0/TabContent/TabContentView.swift
git commit -m "$(cat <<'EOF'
feat(tabcontent): inject git viewer command for kind=.git tabs

resolvedStartupCommand now returns "{mux0-git-viewer}\n" for the
first terminal of any git tab, falling back to "lazygit" when the
setting is empty. Restart-driven re-materialization re-runs the
viewer; splits within the git tab don't.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: Tab pill leading SF Symbol icon

**Files:**
- Modify: `mux0/TabContent/TabBarView.swift`

给 `TabItemView`（private NSView，定义在同文件下半部）加一个 leading `NSImageView`，根据 `tab.kind` 切 SF Symbol：git → `arrow.triangle.branch`，nil → `terminal`。颜色跟随 `titleLabel.textColor`。

- [ ] **Step 1: 在 TabItemView 加 NSImageView 字段 + setup**

打开 `mux0/TabContent/TabBarView.swift`，定位到 `TabItemView` 的字段区（在 `private let titleLabel = NSTextField(labelWithString: "")` 那块）：

把：

```swift
    private let pillView   = NSView()
    private let titleLabel = NSTextField(labelWithString: "")
    private let renameField = NSTextField()
    private let statusIcon = TerminalStatusIconView(frame: .zero)
```

改成：

```swift
    private let pillView   = NSView()
    private let kindIcon   = NSImageView()
    private let titleLabel = NSTextField(labelWithString: "")
    private let renameField = NSTextField()
    private let statusIcon = TerminalStatusIconView(frame: .zero)

    /// 当前显示的 kind —— 用来在 refresh() 时判断是否需要换 image。
    private var displayedKind: TabKind?
```

在 init 入参上加 `kind`：找到 `TabItemView.init(...)`：

```swift
    init(tab: TerminalTab, isSelected: Bool, theme: AppTheme, status: TerminalStatus = .neverRan, backgroundOpacity: CGFloat = 1.0, showStatusIndicators: Bool = false) {
        self.tabId = tab.id
        self.isSelected = isSelected
        self.theme = theme
        self.backgroundOpacity = backgroundOpacity
        self.status = status
        super.init(frame: .zero)
        self.showStatusIndicators = showStatusIndicators
        titleLabel.stringValue = tab.title
        setup()
        updateStyle()
        statusIcon.isHidden = !showStatusIndicators
        statusIcon.update(status: status, theme: theme)
    }
```

替换为：

```swift
    init(tab: TerminalTab, isSelected: Bool, theme: AppTheme, status: TerminalStatus = .neverRan, backgroundOpacity: CGFloat = 1.0, showStatusIndicators: Bool = false) {
        self.tabId = tab.id
        self.isSelected = isSelected
        self.theme = theme
        self.backgroundOpacity = backgroundOpacity
        self.status = status
        self.displayedKind = tab.kind
        super.init(frame: .zero)
        self.showStatusIndicators = showStatusIndicators
        titleLabel.stringValue = tab.title
        setup()
        applyKindImage(tab.kind)
        updateStyle()
        statusIcon.isHidden = !showStatusIndicators
        statusIcon.update(status: status, theme: theme)
    }
```

在 `setup()` 函数里，`pillView.layer?.masksToBounds = true` 之后、`addSubview(statusIcon)` 之前，插入：

```swift
        kindIcon.symbolConfiguration = NSImage.SymbolConfiguration(pointSize: 11, weight: .regular)
        kindIcon.imageScaling = .scaleProportionallyUpOrDown
        addSubview(kindIcon)
```

- [ ] **Step 2: 加 `applyKindImage` 方法**

在 `private func updateStyle()` **之前**插入：

```swift
    private func applyKindImage(_ kind: TabKind?) {
        let symbolName: String = (kind == .git) ? "arrow.triangle.branch" : "terminal"
        kindIcon.image = NSImage(systemSymbolName: symbolName, accessibilityDescription: nil)
    }
```

- [ ] **Step 3: 改 `layout()` —— title 让位给 leading icon**

定位到 `override func layout() { ... }`。把：

```swift
    override func layout() {
        super.layout()
        let h = bounds.height
        let vInset = TabBarView.pillInset
        let pillH = h - vInset * 2
        pillView.frame = NSRect(x: 0, y: vInset, width: bounds.width, height: pillH)
        pillView.layer?.cornerRadius = TabBarView.pillRadius

        let margin: CGFloat = 10
        let iconSize: CGFloat = showStatusIndicators ? TerminalStatusIconView.size : 0
        let iconGap: CGFloat = showStatusIndicators ? 6 : 0
        if showStatusIndicators {
            statusIcon.frame = NSRect(
                x: bounds.width - margin - iconSize, y: (h - iconSize) / 2,
                width: iconSize, height: iconSize)
        }

        let textH = ceil(titleLabel.intrinsicContentSize.height)
        let textX = margin
        let textFrame = NSRect(x: textX, y: (h - textH) / 2,
                               width: bounds.width - margin - iconSize - iconGap - textX,
                               height: textH)
        titleLabel.frame = textFrame
        renameField.frame = textFrame
    }
```

替换为：

```swift
    override func layout() {
        super.layout()
        let h = bounds.height
        let vInset = TabBarView.pillInset
        let pillH = h - vInset * 2
        pillView.frame = NSRect(x: 0, y: vInset, width: bounds.width, height: pillH)
        pillView.layer?.cornerRadius = TabBarView.pillRadius

        let margin: CGFloat = 10
        let trailingIconSize: CGFloat = showStatusIndicators ? TerminalStatusIconView.size : 0
        let trailingGap: CGFloat = showStatusIndicators ? 6 : 0
        if showStatusIndicators {
            statusIcon.frame = NSRect(
                x: bounds.width - margin - trailingIconSize, y: (h - trailingIconSize) / 2,
                width: trailingIconSize, height: trailingIconSize)
        }

        // Leading kind icon: 11pt SF Symbol, fixed gap to title.
        let leadingIconSize: CGFloat = 11
        let leadingGap: CGFloat = 6
        kindIcon.frame = NSRect(
            x: margin, y: (h - leadingIconSize) / 2,
            width: leadingIconSize, height: leadingIconSize)

        let textH = ceil(titleLabel.intrinsicContentSize.height)
        let textX = margin + leadingIconSize + leadingGap
        let textFrame = NSRect(
            x: textX, y: (h - textH) / 2,
            width: bounds.width - margin - trailingIconSize - trailingGap - textX,
            height: textH)
        titleLabel.frame = textFrame
        renameField.frame = textFrame
    }
```

- [ ] **Step 4: 改 `refresh()` —— kind 变化时换图**

定位到 `func refresh(tab: TerminalTab, ...)`：

```swift
    func refresh(tab: TerminalTab, isSelected: Bool, theme: AppTheme,
                 canClose: Bool, status: TerminalStatus = .neverRan,
                 backgroundOpacity: CGFloat = 1.0,
                 showStatusIndicators: Bool = false) {
        self.theme = theme
        self.backgroundOpacity = backgroundOpacity
        self.isSelected = isSelected
        self.canClose = canClose
        self.status = status
        if titleLabel.stringValue != tab.title && !isRenaming {
            titleLabel.stringValue = tab.title
        }
        self.showStatusIndicators = showStatusIndicators
        statusIcon.isHidden = !showStatusIndicators
        statusIcon.update(status: status, theme: theme)
        updateStyle()
        needsLayout = true
    }
```

替换为：

```swift
    func refresh(tab: TerminalTab, isSelected: Bool, theme: AppTheme,
                 canClose: Bool, status: TerminalStatus = .neverRan,
                 backgroundOpacity: CGFloat = 1.0,
                 showStatusIndicators: Bool = false) {
        self.theme = theme
        self.backgroundOpacity = backgroundOpacity
        self.isSelected = isSelected
        self.canClose = canClose
        self.status = status
        if titleLabel.stringValue != tab.title && !isRenaming {
            titleLabel.stringValue = tab.title
        }
        if displayedKind != tab.kind {
            displayedKind = tab.kind
            applyKindImage(tab.kind)
        }
        self.showStatusIndicators = showStatusIndicators
        statusIcon.isHidden = !showStatusIndicators
        statusIcon.update(status: status, theme: theme)
        updateStyle()
        needsLayout = true
    }
```

- [ ] **Step 5: 让 kindIcon 颜色跟随 titleLabel.textColor**

在 `updateStyle()` 末尾（在 `needsDisplay = true` 之前）加一行：

```swift
        kindIcon.contentTintColor = titleLabel.textColor
```

- [ ] **Step 6: 编译并跑全量 test**

```bash
xcodebuild -project mux0.xcodeproj -scheme mux0 -configuration Debug build
xcodebuild test -project mux0.xcodeproj -scheme mux0Tests
```

期望：build SUCCESS、测试全 PASS。

- [ ] **Step 7: 提交**

```bash
git add mux0/TabContent/TabBarView.swift
git commit -m "$(cat <<'EOF'
feat(tabcontent): add leading SF Symbol icon to tab pills

terminal tabs show a `terminal` glyph; git tabs show
`arrow.triangle.branch`. Icon tint follows title color
(textPrimary on selected, textSecondary otherwise).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: 右上角 git 图标按钮

**Files:**
- Modify: `mux0/ContentView.swift`

按 spec：在 ZStack 里以 topTrailing 锚点放一个 IconButton；点击调 `store.ensureGitTab` + `pwdStore.inherit`。

- [ ] **Step 1: 在 ContentView 加 gitTabButton 计算属性**

打开 `mux0/ContentView.swift`，找到现有 `private var sidebarToggleButton: some View { ... }`（约 265–278 行）。在它之后插入：

```swift
    private var gitTabButton: some View {
        IconButton(
            theme: themeManager.theme,
            help: String(localized: L10n.Topbar.gitButtonTooltip.withLocale(locale))
        ) {
            guard let wsId = store.selectedId else { return }
            let result = store.ensureGitTab(in: wsId)
            if result.isNew, let prev = result.sourcePwdTerminalId {
                pwdStore.inherit(from: prev, to: result.terminalId)
            }
        } label: {
            Image(systemName: "arrow.triangle.branch")
                .font(.system(size: 13, weight: .regular))
                .foregroundColor(Color(themeManager.theme.textSecondary))
        }
        .disabled(store.selectedId == nil)
    }
```

- [ ] **Step 2: 把 gitTabButton 挂到 ZStack overlay**

定位到 `body`：

```swift
            sidebarToggleButton
                .padding(.leading, sidebarToggleLeading)
                .padding(.top, DT.Space.xs)
        }
        .frame(minWidth: 960, minHeight: 620)
```

替换为：

```swift
            sidebarToggleButton
                .padding(.leading, sidebarToggleLeading)
                .padding(.top, DT.Space.xs)

            gitTabButton
                .frame(maxWidth: .infinity, alignment: .topTrailing)
                .padding(.trailing, cardInset + DT.Space.xs)
                .padding(.top, DT.Space.xs)
        }
        .frame(minWidth: 960, minHeight: 620)
```

> `cardInset + DT.Space.xs` 的 trailing 让按钮中心大致与卡片右上圆角对齐（cardInset=8 是右侧卡片到窗口的距离；再加一档 xs 内缩，让按钮不贴边）。视觉上若再调实际像素值再让用户反馈一次，这里只确保它在合理范围。

- [ ] **Step 3: 编译验证**

```bash
xcodebuild -project mux0.xcodeproj -scheme mux0 -configuration Debug build
```

期望：build SUCCESS。

- [ ] **Step 4: 跑测试**

```bash
xcodebuild test -project mux0.xcodeproj -scheme mux0Tests
```

期望：所有 case PASS。

- [ ] **Step 5: 提交**

```bash
git add mux0/ContentView.swift
git commit -m "$(cat <<'EOF'
feat(bridge): add top-right git icon button in ContentView

Symmetric to the existing sidebar toggle. Click invokes
WorkspaceStore.ensureGitTab and inherits pwd from the
previously focused terminal so lazygit lands in the right repo.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: 文档同步 + 手动验证

**Files:**
- Modify: `CLAUDE.md`
- Modify: `docs/architecture.md`
- Run: `./scripts/check-doc-drift.sh`

- [ ] **Step 1: CLAUDE.md —— Common Tasks 表追加一行**

打开 `CLAUDE.md`，定位到 `## Common Tasks` 表。在合适位置（Tab/Split 相关行之后）插入：

```markdown
| 增加新的 tab 类型 (TabKind) | `Models/Workspace.swift`（加 enum case），`Models/WorkspaceStore.swift`（addTab kind 透传 + ensure*Tab helper），`TabContent/TabContentView.swift`（resolvedStartupCommand 注入），`TabContent/TabBarView.swift`（kindIcon 映射），`ContentView.swift`（顶部入口按钮），i18n 与 settings 同步 |
```

- [ ] **Step 2: docs/architecture.md —— Tab content 章节加 git tab 行为**

打开 `docs/architecture.md`，找到讲 Tab content / SplitNode 的章节，在合适位置追加一段：

```markdown
### Git Tab

`TerminalTab.kind == .git` 标记一种语义化 tab：右上角 git 图标按钮通过 `WorkspaceStore.ensureGitTab(in:)` 创建（或切到现有），title 默认 `"Git"`，allow rename。每个 workspace 最多一个。

启动命令注入路径：`TabContentView.resolvedStartupCommand(forTerminal:)` 检测到 `tab.kind == .git` 且 `id == tab.layout.allTerminalIds().first` 时，从 `mux0-git-viewer` setting 读命令（默认 `lazygit`）作为 ghostty surface 的 `initial_input`。意味着重启 mux0 → surface 重建 → viewer 自动重跑；split 出的次级 pane 不会再跑（它不是 layout 第一个终端）。

视觉上 tab pill 加了 leading SF Symbol：git tab → `arrow.triangle.branch`，普通 terminal tab → `terminal`，颜色跟随 title 文字色。
```

- [ ] **Step 3: 跑 check-doc-drift**

```bash
./scripts/check-doc-drift.sh
```

期望：无 drift 报错（脚本会比对 CLAUDE.md / AGENTS.md 的 Directory Structure 与真实 `mux0/` 目录）。

> 如果脚本提示 AGENTS.md 也要同步更新，按提示同步——AGENTS.md 通常镜像 CLAUDE.md。

- [ ] **Step 4: 手动验证清单（用户启动 mux0 后过一遍）**

在已完成所有代码改动并 build 成功的前提下，记录一份要测的列表给用户。**不要主动 `open mux0.app`**（CLAUDE.md 明确禁止），由用户自己启动验证。

清单：

1. 启动 mux0 → 右上角看到 `arrow.triangle.branch` 图标按钮，所有现有 tab 的 pill 都有 leading `terminal` SF Symbol。
2. 在某个 cd 进 git 仓库的 pane 里点右上角 git 图标 → 新建 "Git" tab，pill 上是 git fork 图标，里面自动跑 lazygit、落在该仓库。
3. 再点一次 git 图标 → tab 不增多，仅切到原 Git tab。
4. 关掉 Git tab 再点 git 图标 → 重新创建（isNew=true 路径）。
5. 在 Git tab 里 ⌘D → 新 pane 是空 shell，不跑 lazygit。
6. 退出 mux0 重启 → Git tab 仍在并自动重跑 lazygit。
7. Settings → Shell → Git viewer command 改成 `tig` → 关闭并重新点 git 图标 → 跑 tig。
8. 清空 Git viewer command → 再开一次 → 跑 lazygit（默认）。
9. 切语言到中文 → 右上角 git 按钮 tooltip 是"打开 Git 视图"，Settings 标签是"Git 工具命令"。

- [ ] **Step 5: 提交文档改动**

```bash
git add CLAUDE.md docs/architecture.md
git commit -m "$(cat <<'EOF'
docs: sync git tab feature into CLAUDE.md and architecture.md

Add a Common Tasks row for adding new tab kinds and a Git Tab
section under tab-content architecture covering kind-aware command
injection, restart behaviour, and pill icon rendering.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Self-review notes

- **Spec coverage**：spec 的所有 sections 都映射到 task 1-8。kind 数据模型 → Task 1，store 方法 → Task 2，命令注入 → Task 5，按钮 UI → Task 7，tab pill icon → Task 6，settings → Task 4，i18n → Task 3，doc → Task 8。
- **类型一致性**：`ensureGitTab` 返回元组在 Task 2 / Task 7 两处使用一致：`(tabId, terminalId, isNew, sourcePwdTerminalId)`。`TabKind` 全程是 `enum TabKind: String, Codable, Equatable { case git }`。
- **顺序依赖**：Task 1 (TabKind) → 2 (store 方法) → 3 (i18n) → 4 (settings UI) → 5 (注入路径) → 6 (pill icon) → 7 (按钮) → 8 (doc)。Task 5 / 6 / 7 共同依赖 Task 1 (kind)；Task 7 依赖 Task 2 (ensureGitTab) 和 Task 3 (L10n.Topbar)；Task 4 依赖 Task 3。所有依赖均按顺序满足。
- **占位符扫描**：每一步都有具体代码 + 具体命令 + 具体期望输出，无 TODO / TBD。
- **不需要也不应该自动 `open mux0.app`**：CLAUDE.md 明确禁止；Task 8 的手动验证由用户执行。
