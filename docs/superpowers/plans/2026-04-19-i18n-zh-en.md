# 中英国际化（i18n）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 Settings > Appearance 新增 Language 行（`System / 中文 / English`），语言持久化到 UserDefaults，切换后 Sidebar / Tab / Settings / 菜单栏 / 错误页所有文案即时刷新。

**Architecture:** `@Observable LanguageStore` 单例持有 `preference` + `tick`，持久化到 UserDefaults；SwiftUI 通过 `.environment(\.locale, store.locale)` + `LocalizedStringResource` 自动重绘；AppKit 子类走 `Bundle.localizedString` 手动查，靠 `*Bridge` 透传 `languageTick` 触发 `updateNSView` 刷新 label。文案集中在 `Strings.xcstrings`（String Catalog），常量层 `L10n` 枚举规避魔法字符串。

**Tech Stack:** Swift 5.9 / SwiftUI + AppKit / Xcode 15 String Catalog (`.xcstrings`) / `@Observable`（Observation framework）。

**Spec:** `docs/superpowers/specs/2026-04-19-i18n-zh-en-design.md`

**Worktree:** `~/.config/superpowers/worktrees/mux0/agent-i18n/`（`agent/i18n-zh-en` 分支，从 master 起）

**通用说明：**
- 每个 task 结束后跑 `xcodebuild test -project mux0.xcodeproj -scheme mux0Tests 2>&1 | tail -20` 确保测试通过。
- commit 格式 `type(i18n): description` —— **第一步会把 `i18n` 加入 CLAUDE.md 合法 scope 列表**。
- 改 `project.yml` 要先征求用户确认（task 2 中显式暂停）。
- 工作目录固定在 worktree：`cd ~/.config/superpowers/worktrees/mux0/agent-i18n` 开头。

---

## File Structure

### 新增文件

| 文件 | 职责 |
|------|------|
| `mux0/Localization/LanguageStore.swift` | `@Observable` 单例 + `Preference` 枚举 + `effectiveBundle` + `locale` + `tick` |
| `mux0/Localization/L10n.swift` | 常量层：`enum L10n { enum Sidebar {…} enum Settings {…} enum Tab {…} enum App {…} enum Menu {…} }`，每 key 暴露 `LocalizedStringResource`；+ AppKit helper `L10n.string(_:args:)` |
| `mux0/Localization/Strings.xcstrings` | String Catalog，english source + 中文 translation |
| `mux0Tests/LanguageStoreTests.swift` | Preference 持久化 / locale 解析 / tick 自增 |
| `mux0Tests/L10nSmokeTests.swift` | 全部 key 在 zh/en bundle 下都能查到非 key 本身的值 |
| `docs/i18n.md` | 开发者指南：如何加 key、如何支持新语言 |

### 修改文件

基础设施 → 视图迁移 → 入口 Picker → app 级 → 文档 / smoke。

| 文件 | 变更 |
|------|------|
| `CLAUDE.md` | scope 列表加 `i18n`；Directory Structure 加 `Localization/`；Common Tasks 加 "新增文案 / 支持新语言"；Documentation Map 加 `docs/i18n.md` |
| `AGENTS.md` | 同 CLAUDE.md（若存在同样字段） |
| `project.yml` | 把 `mux0/Localization/Strings.xcstrings` 作为资源纳入 mux0 target（若 xcodegen 没自动发现） |
| `mux0/mux0App.swift` | 实例化 `LanguageStore`；错误页文案 / Commands 菜单项走 L10n |
| `mux0/ContentView.swift` | `.environment(\.locale, languageStore.locale)` + 向下注入 `languageStore` |
| `mux0/Sidebar/SidebarView.swift` | 所有 `Text("…")` / tooltip / alert 走 L10n；向 bridge 传 `languageTick` |
| `mux0/Sidebar/WorkspaceListView.swift` | 若有静态文案（上下文菜单/空态），走 L10n；提供 `refreshLocalizedStrings()` |
| `mux0/Bridge/SidebarListBridge.swift` | 新增 `languageTick: Int` 参数，在 `updateNSView` 里触发 refresh |
| `mux0/Bridge/TabBridge.swift` | 同上 |
| `mux0/TabContent/TabBarView.swift` | tooltip 走 L10n；`refreshLocalizedStrings()` |
| `mux0/TabContent/TabContentView.swift` | 若有静态文案；把 languageTick 透传到子 SplitPaneView |
| `mux0/Settings/SettingsView.swift` | footer 两条文案 |
| `mux0/Settings/SettingsSection.swift` | `label` 返回 `LocalizedStringResource` |
| `mux0/Settings/SettingsTabBarView.swift` | "Close settings" tooltip |
| `mux0/Settings/Sections/AppearanceSectionView.swift` | **新增 Language Picker 行** + 现有字段走 L10n |
| `mux0/Settings/Sections/FontSectionView.swift` | 字段 label |
| `mux0/Settings/Sections/TerminalSectionView.swift` | 字段 label |
| `mux0/Settings/Sections/ShellSectionView.swift` | 字段 label |
| `mux0/Settings/Components/SettingsResetRow.swift` | "All settings in this section…" |
| `mux0/Settings/Components/FontPickerView.swift` | "(default)" / "Custom…" |
| `docs/architecture.md` | 新增 Localization 章节 |

---

## Task 1: 把 `i18n` 加入 CLAUDE.md 的合法 scope 列表

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: 编辑 CLAUDE.md 的 scope 列表**

在 `## Key Conventions` 的第 5 条，把 `update` 后面加上 `| i18n`：

```markdown
5. **提交格式** `type(scope): description` — e.g. `feat(tabcontent): add drag-to-reorder`；scope 与目录对应，合法值：`sidebar | tabcontent | settings | theme | ghostty | models | metadata | bridge | build | docs | update | i18n`
```

（**Note:** 别的字段（Directory Structure / Common Tasks）在 task 11 再一并补，这样此时 commit 只做 scope 扩容，原子提交。）

- [ ] **Step 2: Commit**

```bash
cd ~/.config/superpowers/worktrees/mux0/agent-i18n
git add CLAUDE.md
git commit -m "docs(i18n): add i18n to allowed commit scope list"
```

---

## Task 2: Scaffold LanguageStore（仅 Preference + persistence + tick，不含 bundle 解析）

**Files:**
- Create: `mux0/Localization/LanguageStore.swift`
- Create: `mux0Tests/LanguageStoreTests.swift`

- [ ] **Step 1: 建目录**

```bash
cd ~/.config/superpowers/worktrees/mux0/agent-i18n
mkdir -p mux0/Localization
```

- [ ] **Step 2: 先写失败测试 `LanguageStoreTests.swift`**

```swift
import XCTest
@testable import mux0

final class LanguageStoreTests: XCTestCase {
    private let testKey = "mux0.language.test"

    override func setUp() {
        super.setUp()
        UserDefaults.standard.removeObject(forKey: testKey)
    }

    override func tearDown() {
        UserDefaults.standard.removeObject(forKey: testKey)
        super.tearDown()
    }

    func testDefaultPreferenceIsSystem() {
        let store = LanguageStore(storageKey: testKey, defaults: .standard)
        XCTAssertEqual(store.preference, .system)
    }

    func testPreferencePersists() {
        let store1 = LanguageStore(storageKey: testKey, defaults: .standard)
        store1.preference = .zh
        let store2 = LanguageStore(storageKey: testKey, defaults: .standard)
        XCTAssertEqual(store2.preference, .zh)
    }

    func testInvalidStoredValueFallsBackToSystem() {
        UserDefaults.standard.set("bogus", forKey: testKey)
        let store = LanguageStore(storageKey: testKey, defaults: .standard)
        XCTAssertEqual(store.preference, .system)
    }

    func testTickIncrementsOnPreferenceChange() {
        let store = LanguageStore(storageKey: testKey, defaults: .standard)
        let before = store.tick
        store.preference = .zh
        XCTAssertEqual(store.tick, before &+ 1)
    }

    func testTickDoesNotIncrementOnSameValue() {
        let store = LanguageStore(storageKey: testKey, defaults: .standard)
        store.preference = .zh
        let mid = store.tick
        store.preference = .zh
        XCTAssertEqual(store.tick, mid)
    }
}
```

- [ ] **Step 3: 运行测试确认失败（类型未定义）**

```bash
xcodebuild test -project mux0.xcodeproj -scheme mux0Tests 2>&1 | tail -30
```
Expected: 编译错误 `cannot find 'LanguageStore' in scope`

- [ ] **Step 4: 写 `LanguageStore.swift` 最小实现**

```swift
import Foundation
import Observation

/// Global i18n store. Holds the user's language preference (system/zh/en),
/// persists it to UserDefaults, and publishes a monotonic `tick` that AppKit
/// bridges read in order to rebuild their NSView labels when language changes.
@Observable
final class LanguageStore {
    /// Language preference. `.system` defers to `Locale.current`.
    enum Preference: String, CaseIterable, Codable, Sendable {
        case system
        case zh
        case en
    }

    static let defaultStorageKey = "mux0.language"
    static let shared = LanguageStore(storageKey: defaultStorageKey, defaults: .standard)

    private let storageKey: String
    private let defaults: UserDefaults

    /// Monotonic counter for AppKit refresh triggers. Increments whenever
    /// `preference` changes. Overflow is intentional (&+=) — the absolute
    /// value doesn't matter, only "did it change".
    private(set) var tick: Int = 0

    var preference: Preference {
        didSet {
            guard preference != oldValue else { return }
            defaults.set(preference.rawValue, forKey: storageKey)
            tick &+= 1
        }
    }

    init(storageKey: String, defaults: UserDefaults) {
        self.storageKey = storageKey
        self.defaults = defaults
        if let raw = defaults.string(forKey: storageKey),
           let pref = Preference(rawValue: raw) {
            self.preference = pref
        } else {
            self.preference = .system
        }
    }
}
```

- [ ] **Step 5: 运行测试确认通过**

```bash
xcodebuild test -project mux0.xcodeproj -scheme mux0Tests 2>&1 | grep -E "(Executed.*tests|\*\* TEST)" | tail -5
```
Expected: `** TEST SUCCEEDED **`，测试数比之前 +5。

- [ ] **Step 6: Commit**

```bash
git add mux0/Localization/LanguageStore.swift mux0Tests/LanguageStoreTests.swift
git commit -m "feat(i18n): add LanguageStore (Preference + persistence + tick)"
```

---

## Task 3: 新增 `Strings.xcstrings` + 征求 project.yml 修改确认

**Files:**
- Create: `mux0/Localization/Strings.xcstrings`
- Maybe modify: `project.yml`（先跑一次 xcodegen 确认是否需要手动加）

- [ ] **Step 1: 用最小 stub 内容创建 `Strings.xcstrings`**

Xcode String Catalog 是 JSON：

```json
{
  "sourceLanguage" : "en",
  "strings" : {
    "sidebar.title" : {
      "extractionState" : "manual",
      "localizations" : {
        "en" : { "stringUnit" : { "state" : "translated", "value" : "mux0" } },
        "zh-Hans" : { "stringUnit" : { "state" : "translated", "value" : "mux0" } }
      }
    }
  },
  "version" : "1.0"
}
```

写到 `mux0/Localization/Strings.xcstrings`。（这是可运行的最小示例，后续 task 再填补所有 key。）

- [ ] **Step 2: 重新生成 Xcode 工程并构建，确认 xcstrings 被识别**

```bash
xcodegen generate 2>&1 | tail -3
xcodebuild build -project mux0.xcodeproj -scheme mux0 -configuration Debug 2>&1 | grep -E "(Strings\.xcstrings|BUILD SUCCEEDED|error:)" | tail -10
```

Expected：看到 `Intents.xcstrings`/`Strings.xcstrings` 被 compiled（日志里会有 `CompileXCStrings` 或拷贝到 `zh-Hans.lproj`），`BUILD SUCCEEDED`。

- [ ] **Step 3: 验证 bundle 里出现 `zh-Hans.lproj` 与 `en.lproj`**

```bash
APP=$(xcodebuild -project mux0.xcodeproj -scheme mux0 -showBuildSettings 2>/dev/null | awk '/TARGET_BUILD_DIR/ {print $3}' | head -1)
ls -la "$APP/mux0.app/Contents/Resources/" | grep lproj
```

Expected：`en.lproj/` 和 `zh-Hans.lproj/`（都包含一个 `.strings` / `.stringsdict` 文件）。

- [ ] **Step 4: 如果 lproj 没出现 → project.yml 调整（需用户确认）**

**暂停**：如果上一步没有 lproj，请人工确认后再修 `project.yml`。
修改方式：在 `targets.mux0` 下显式加一条：

```yaml
targets:
  mux0:
    # ...
    sources:
      - path: mux0
        excludes:
          - "**/*.md"
      - path: mux0/Localization/Strings.xcstrings  # 显式以保证识别
        type: file
        buildPhase: resources
```

然后重新 `xcodegen generate` 并再构建验证。

（若自动识别通过就跳过这一步。）

- [ ] **Step 5: Commit**

```bash
git add mux0/Localization/Strings.xcstrings
git add project.yml mux0.xcodeproj 2>/dev/null || true
git commit -m "feat(i18n): add Strings.xcstrings catalog (stub)"
```

---

## Task 4: 加 `effectiveBundle` + `locale` 派生属性 + 测试

**Files:**
- Modify: `mux0/Localization/LanguageStore.swift`
- Modify: `mux0Tests/LanguageStoreTests.swift`

- [ ] **Step 1: 先加失败测试**

在 `LanguageStoreTests` 末尾加：

```swift
func testLocaleForZh() {
    let store = LanguageStore(storageKey: testKey, defaults: .standard)
    store.preference = .zh
    XCTAssertEqual(store.locale.identifier, "zh-Hans")
}

func testLocaleForEn() {
    let store = LanguageStore(storageKey: testKey, defaults: .standard)
    store.preference = .en
    XCTAssertEqual(store.locale.identifier, "en")
}

func testLocaleForSystemMatchesCurrent() {
    let store = LanguageStore(storageKey: testKey, defaults: .standard)
    store.preference = .system
    XCTAssertEqual(store.locale, Locale.current)
}

func testEffectiveBundleForZhReturnsZhHansLproj() {
    let store = LanguageStore(storageKey: testKey, defaults: .standard)
    store.preference = .zh
    // 通过查一个已在 Catalog 里 translate 过的 key 验证 bundle 定位正确
    let value = store.effectiveBundle.localizedString(forKey: "sidebar.title", value: nil, table: nil)
    // "mux0" is same in both locales for the stub; what we verify is we got
    // a non-empty string back (bundle resolved) and the bundlePath contains "zh-Hans"
    XCTAssertFalse(value.isEmpty)
    XCTAssertTrue(store.effectiveBundle.bundlePath.contains("zh-Hans.lproj"))
}

func testEffectiveBundleForEnReturnsEnLproj() {
    let store = LanguageStore(storageKey: testKey, defaults: .standard)
    store.preference = .en
    XCTAssertTrue(store.effectiveBundle.bundlePath.contains("en.lproj"))
}
```

- [ ] **Step 2: 运行，确认失败**

```bash
xcodebuild test -project mux0.xcodeproj -scheme mux0Tests 2>&1 | tail -20
```
Expected：编译错 `value of type 'LanguageStore' has no member 'locale'` / `effectiveBundle`。

- [ ] **Step 3: 实现**

在 `LanguageStore.swift` 末尾（结构体 body 内）加：

```swift
    /// The locale to inject into SwiftUI's `\.locale` environment.
    var locale: Locale {
        switch preference {
        case .system: return .current
        case .zh:     return Locale(identifier: "zh-Hans")
        case .en:     return Locale(identifier: "en")
        }
    }

    /// Bundle to hand to `Bundle.localizedString(forKey:value:table:)` for AppKit
    /// code paths that can't use SwiftUI's `\.locale` environment.
    /// Falls back to the parent bundle if the lproj directory can't be resolved.
    var effectiveBundle: Bundle {
        let parent = Bundle(for: LanguageStore.self)
        let code: String
        switch preference {
        case .system:
            code = Locale.current.language.languageCode?.identifier == "zh" ? "zh-Hans" : "en"
        case .zh: code = "zh-Hans"
        case .en: code = "en"
        }
        guard let path = parent.path(forResource: code, ofType: "lproj"),
              let bundle = Bundle(path: path)
        else { return parent }
        return bundle
    }
```

- [ ] **Step 4: 运行测试确认通过**

```bash
xcodebuild test -project mux0.xcodeproj -scheme mux0Tests 2>&1 | grep -E "(Executed.*tests|\*\* TEST)" | tail -5
```
Expected：`** TEST SUCCEEDED **`。

- [ ] **Step 5: Commit**

```bash
git add mux0/Localization/LanguageStore.swift mux0Tests/LanguageStoreTests.swift
git commit -m "feat(i18n): add LanguageStore.locale and effectiveBundle"
```

---

## Task 5: 加 `L10n` 常量层

**Files:**
- Create: `mux0/Localization/L10n.swift`

- [ ] **Step 1: 写文件**

完整内容：

```swift
import Foundation

/// Compile-time namespace for all localizable strings. Organized by UI module.
///
/// - SwiftUI: use the `LocalizedStringResource` properties directly
///   → `Text(L10n.Sidebar.newWorkspace)`.
/// - AppKit:  use `L10n.string("key", args...)` helper → returns a `String`
///   resolved against `LanguageStore.shared.effectiveBundle`.
///
/// Keys are dotted namespaces mirroring UI structure; source language is English,
/// translations live in `mux0/Localization/Strings.xcstrings`.
enum L10n {
    // MARK: - AppKit helper

    /// Resolve `key` against the shared store's effective bundle. If `args`
    /// is non-empty, `String(format:)` the result. Intended for AppKit-side
    /// `stringValue`/`toolTip` assignments that can't use SwiftUI's `\.locale`.
    static func string(_ key: String, _ args: CVarArg...) -> String {
        let bundle = LanguageStore.shared.effectiveBundle
        let raw = bundle.localizedString(forKey: key, value: nil, table: nil)
        return args.isEmpty ? raw : String(format: raw, arguments: args)
    }

    // MARK: - Sidebar

    enum Sidebar {
        static let title                = LocalizedStringResource("sidebar.title")
        static let newWorkspace         = LocalizedStringResource("sidebar.newWorkspace")
        static let settingsTooltip      = LocalizedStringResource("sidebar.settings")
        static let deleteAlertTitle     = LocalizedStringResource("sidebar.deleteAlert.title")
        static let deleteAlertCancel    = LocalizedStringResource("sidebar.deleteAlert.cancel")
        static let deleteAlertConfirm   = LocalizedStringResource("sidebar.deleteAlert.confirm")
        static func deleteAlertMessage(_ name: String) -> LocalizedStringResource {
            // Note: at runtime `LocalizedStringResource` does not auto-substitute
            // positional args; consumers use `String(localized: res)` +
            // `String(format:)`, or just use `L10n.string(...)` for AppKit.
            LocalizedStringResource("sidebar.deleteAlert.message \(name)")
        }
        static let showSidebar          = LocalizedStringResource("sidebar.show")
        static let hideSidebar          = LocalizedStringResource("sidebar.hide")
    }

    // MARK: - Tab

    enum Tab {
        static let newTabTooltip    = LocalizedStringResource("tab.newTab")
    }

    // MARK: - Settings

    enum Settings {
        // Sections
        static let sectionAppearance    = LocalizedStringResource("settings.section.appearance")
        static let sectionFont          = LocalizedStringResource("settings.section.font")
        static let sectionTerminal      = LocalizedStringResource("settings.section.terminal")
        static let sectionShell         = LocalizedStringResource("settings.section.shell")

        // Shell (chrome)
        static let close                = LocalizedStringResource("settings.close")
        static let footerEdit           = LocalizedStringResource("settings.footer.edit")
        static let footerLive           = LocalizedStringResource("settings.footer.live")

        // Appearance fields
        static let theme                = LocalizedStringResource("settings.appearance.theme")
        static let opacity              = LocalizedStringResource("settings.appearance.opacity")
        static let blur                 = LocalizedStringResource("settings.appearance.blur")
        static let language             = LocalizedStringResource("settings.appearance.language")
        static let languageSystem       = LocalizedStringResource("settings.language.system")
        // Note: .zh / .en option labels are hard-coded in the UI (never translated):
        //       "中文（简体）" shows in Chinese, "English" shows in English.

        // Font
        static let fontFamily           = LocalizedStringResource("settings.font.family")
        static let fontSize             = LocalizedStringResource("settings.font.size")
        static let fontDefault          = LocalizedStringResource("settings.font.default")
        static let fontCustom           = LocalizedStringResource("settings.font.custom")

        // Reset row
        static let resetTitle           = LocalizedStringResource("settings.reset.title")
        static let resetMessage         = LocalizedStringResource("settings.reset.message")
        static let resetButton          = LocalizedStringResource("settings.reset.button")

        // Terminal / Shell fields (add as we migrate each Section's labels)
        enum Terminal {
            // filled in during Task 8; keep placeholder namespace
        }
        enum Shell {
            // filled in during Task 8
        }
    }

    // MARK: - App

    enum App {
        static let ghosttyNotFoundTitle     = LocalizedStringResource("app.ghostty.notFound.title")
        static let ghosttyNotFoundDetail    = LocalizedStringResource("app.ghostty.notFound.detail")
    }

    // MARK: - Menu

    enum Menu {
        static let settings             = LocalizedStringResource("menu.settings")
        static let editConfig           = LocalizedStringResource("menu.editConfig")
        static let newWorkspace         = LocalizedStringResource("menu.newWorkspace")
        static let newTab               = LocalizedStringResource("menu.newTab")
        static let closePane            = LocalizedStringResource("menu.closePane")
        static let splitVertical        = LocalizedStringResource("menu.splitVertical")
        static let splitHorizontal      = LocalizedStringResource("menu.splitHorizontal")
        static let focusNextPane        = LocalizedStringResource("menu.focusNextPane")
        static let focusPrevPane        = LocalizedStringResource("menu.focusPrevPane")
        static let selectNextTab        = LocalizedStringResource("menu.selectNextTab")
        static let selectPrevTab        = LocalizedStringResource("menu.selectPrevTab")
        static let terminalMenu         = LocalizedStringResource("menu.terminal")
        static let copy                 = LocalizedStringResource("menu.copy")
        static let paste                = LocalizedStringResource("menu.paste")
        static let selectAll            = LocalizedStringResource("menu.selectAll")
        static let help                 = LocalizedStringResource("menu.help")
        /// `%d` will be formatted at call site in mux0App.
        static func selectTabN(_ n: Int) -> LocalizedStringResource {
            LocalizedStringResource("menu.selectTab \(n)")
        }
    }
}
```

- [ ] **Step 2: 编译验证**

```bash
xcodebuild build -project mux0.xcodeproj -scheme mux0 -configuration Debug 2>&1 | grep -E "(BUILD|error:)" | tail -5
```
Expected：`** BUILD SUCCEEDED **`。

- [ ] **Step 3: Commit**

```bash
git add mux0/Localization/L10n.swift
git commit -m "feat(i18n): add L10n constants namespace"
```

---

## Task 6: 把 `LanguageStore` 注入到 mux0App + ContentView

**Files:**
- Modify: `mux0/mux0App.swift`
- Modify: `mux0/ContentView.swift`

此 task 目的：让 `@Observable LanguageStore` 的变化能在 SwiftUI 树内通过 `.environment` 被订阅，同时用 `\.locale` 环境让所有 `LocalizedStringResource` 自动跟随。**此步不迁移任何现有硬编码文案。**

- [ ] **Step 1: `mux0App.swift` 加 `@State private var languageStore = LanguageStore.shared` + 注入**

改动位置（第 7 行附近）：

```swift
@main
struct mux0App: App {
    @NSApplicationDelegateAdaptor(AppDelegate.self) private var appDelegate
    @State private var themeManager = ThemeManager()
    @State private var languageStore = LanguageStore.shared   // ← 新增
    // ...

    var body: some Scene {
        WindowGroup {
            if GhosttyBridge.shared.isInitialized {
                ContentView()
                    .environment(themeManager)
                    .environment(languageStore)                    // ← 新增
                    .environment(\.locale, languageStore.locale)   // ← 新增
            } else {
                GhosttyMissingView()
                    .environment(languageStore)                    // ← 新增
                    .environment(\.locale, languageStore.locale)   // ← 新增
            }
        }
        // ...
    }
}
```

- [ ] **Step 2: `ContentView.swift` 加 `@Environment(LanguageStore.self) private var languageStore`**

只是声明，为 task 7+ 的视图迁移备好引用。在 `@Environment(ThemeManager.self)` 后一行：

```swift
@Environment(ThemeManager.self) private var themeManager
@Environment(LanguageStore.self) private var languageStore   // ← 新增
```

- [ ] **Step 3: Build 验证**

```bash
xcodebuild build -project mux0.xcodeproj -scheme mux0 2>&1 | grep -E "(BUILD|error:)" | tail -5
```
Expected：`BUILD SUCCEEDED`。

- [ ] **Step 4: 运行测试（确保没破坏现有测试）**

```bash
xcodebuild test -project mux0.xcodeproj -scheme mux0Tests 2>&1 | grep -E "(Executed.*tests|\*\* TEST)" | tail -5
```
Expected：全绿。

- [ ] **Step 5: Commit**

```bash
git add mux0/mux0App.swift mux0/ContentView.swift
git commit -m "feat(i18n): inject LanguageStore + locale env into mux0App / ContentView"
```

---

## Task 7: 迁移 Sidebar 文案到 L10n（含 Catalog 补键）

**Files:**
- Modify: `mux0/Localization/Strings.xcstrings`
- Modify: `mux0/Sidebar/SidebarView.swift`
- Modify: `mux0/Bridge/SidebarListBridge.swift`
- Modify: `mux0/Sidebar/WorkspaceListView.swift`
- Modify: `mux0/ContentView.swift`（sidebar toggle tooltip）

- [ ] **Step 1: 给 Catalog 补齐本 task 涉及的 key**

在 `Strings.xcstrings` 的 `strings` object 里追加（保持 alphabetical 排序利于 diff）：

| key | en | zh-Hans |
|-----|----|---------|
| `sidebar.deleteAlert.cancel` | "Cancel" | "取消" |
| `sidebar.deleteAlert.confirm` | "Delete" | "删除" |
| `sidebar.deleteAlert.message %@` | "“%@” will be deleted with all its tabs. This action cannot be undone." | "「%@」及其所有 tab 将被删除，此操作不可撤销。" |
| `sidebar.deleteAlert.title` | "Delete workspace?" | "删除 workspace？" |
| `sidebar.newWorkspace` | "New workspace" | "新建 workspace" |
| `sidebar.settings` | "Settings" | "设置" |
| `sidebar.show` | "Show sidebar" | "展开侧栏" |
| `sidebar.hide` | "Hide sidebar" | "收起侧栏" |
| `sidebar.title` | "mux0" | "mux0" |

**提示**：推荐用 Xcode 的 Catalog UI 编辑（打开 `.xcstrings` 文件会有表格视图）；纯文本编辑时请严格遵循 JSON，必要的结构见 task 3 的 stub。

- [ ] **Step 2: 改 `SidebarView.swift`**

替换位置 & 具体改动：

```swift
// footer (line 75-88)
private var footer: some View {
    HStack {
        IconButton(theme: theme, help: String(localized: L10n.Sidebar.settingsTooltip)) {  // ← 改 help 参数
            NotificationCenter.default.post(name: .mux0OpenSettings, object: nil)
        } label: {
            Image(systemName: "gearshape")
                // ...
        }
        Spacer()
    }
    // ...
}
```

```swift
// header (line 92-108)
private var header: some View {
    HStack(spacing: DT.Space.sm) {
        Text(L10n.Sidebar.title)                                                          // ← 改
            .font(Font(DT.Font.title))
            .foregroundColor(Color(theme.textPrimary))
        Spacer()
        IconButton(theme: theme, help: String(localized: L10n.Sidebar.newWorkspace)) {    // ← 改
            createWorkspaceWithDefaultName()
        } label: {
            Text("+")
                .font(Font(DT.Font.body))
                .foregroundColor(Color(theme.textSecondary))
        }
    }
    // ...
}
```

```swift
// alert (line 54-70)
.alert(String(localized: L10n.Sidebar.deleteAlertTitle),                                  // ← 改
       isPresented: Binding(
           get: { workspaceToDelete != nil },
           set: { if !$0 { workspaceToDelete = nil } })) {
    Button(String(localized: L10n.Sidebar.deleteAlertCancel), role: .cancel) {            // ← 改
        workspaceToDelete = nil
    }
    Button(String(localized: L10n.Sidebar.deleteAlertConfirm), role: .destructive) {      // ← 改
        if let id = workspaceToDelete { store.deleteWorkspace(id: id) }
        workspaceToDelete = nil
    }
} message: {
    if let id = workspaceToDelete,
       let ws = store.workspaces.first(where: { $0.id == id }) {
        // Use L10n.string (AppKit helper) with %@ interpolation — SwiftUI alert `message`
        // accepts Text, but we do the formatting manually since the key has a %@.
        Text(L10n.string("sidebar.deleteAlert.message %@", ws.name))                       // ← 改
    }
}
```

`IconButton` 的 `help` 参数原签名是 `String?`，`String(localized:)` 传 `String`。若 IconButton 的 help 不是 String 而是别的类型，沿用原型。

- [ ] **Step 3: 改 `SidebarListBridge.swift` — 新增 `languageTick` 参数**

定位 `struct SidebarListBridge: NSViewRepresentable` 签名，加字段：

```swift
struct SidebarListBridge: NSViewRepresentable {
    // ... existing fields
    var languageTick: Int         // ← 新增；忽略其值，读取它只为了让 SwiftUI 在 tick 变化时
                                  //   重新跑 updateNSView
    // ...
}
```

并在 `updateNSView(_ nsView: ..., context:)` 里，在方法末尾加：

```swift
// 语言切换时让内部 view 重读所有 stringValue/toolTip
nsView.refreshLocalizedStrings()   // 见 task 7 step 4
```

- [ ] **Step 4: 改 `WorkspaceListView.swift` — 提供 `refreshLocalizedStrings()`**

先用 `Grep` 扫一眼 `WorkspaceListView.swift` 里的硬编码文案：

```bash
grep -n 'stringValue\|toolTip\|placeholderString\|Text(\"' mux0/Sidebar/WorkspaceListView.swift
```

对找到的每处硬编码文案：
1. 把字面量替换成 `L10n.string("key")`（先去 Catalog 加对应 key）。
2. 在类末尾加一个 `func refreshLocalizedStrings()` 方法，里面重新对所有 label 赋值（枚举每个 view reference）。
3. 在 bridge 的 `updateNSView` 里调这个方法（Step 3 已准备好）。

**如果 grep 没找到任何硬编码文案（只展示 workspace.name / metadata.branch 等 dynamic 数据）**，仍然加一个空壳方法（给 task 后续仍可 extend）：

```swift
extension WorkspaceListView {
    /// Called from the bridge when `LanguageStore.tick` changes. Safe no-op
    /// if there are no static strings in this view.
    func refreshLocalizedStrings() {
        // Currently the list rows display only user-given workspace.name and
        // dynamic metadata; no static labels to re-resolve. Placeholder for
        // when we add a tooltip or context-menu item.
    }
}
```

- [ ] **Step 5: 在 `SidebarView` 里向 bridge 透传 `languageTick`**

```swift
@Environment(LanguageStore.self) private var languageStore    // ← 加（如果还没在此文件）

// ...

SidebarListBridge(
    store: store,
    statusStore: statusStore,
    theme: theme,
    metadata: metadataMap,
    metadataTick: metadataTicker.tick,
    backgroundOpacity: backgroundOpacity,
    showStatusIndicators: showStatusIndicators,
    languageTick: languageStore.tick,                         // ← 新增
    onRequestDelete: { workspaceToDelete = $0 }
)
```

- [ ] **Step 6: `ContentView.swift` sidebar toggle button tooltip 走 L10n**

```swift
private var sidebarToggleButton: some View {
    IconButton(
        theme: themeManager.theme,
        help: String(localized: sidebarCollapsed ? L10n.Sidebar.showSidebar : L10n.Sidebar.hideSidebar)   // ← 改
    ) {
        // ...
    } label: {
        // ...
    }
}
```

- [ ] **Step 7: Build + test**

```bash
xcodebuild build -project mux0.xcodeproj -scheme mux0 2>&1 | grep -E "(BUILD|error:)" | tail -5
xcodebuild test -project mux0.xcodeproj -scheme mux0Tests 2>&1 | grep -E "(Executed.*tests|\*\* TEST)" | tail -5
```
Expected: BUILD SUCCEEDED, 全部测试通过。

- [ ] **Step 8: 人工测试（运行 app，切 `LanguageStore.shared.preference = .zh` 验证）**

临时 debug 方式：在 `ContentView.onAppear` 里加一行 `languageStore.preference = .zh` 跑 debug build 看 sidebar 文案是否中文，验完删掉；或者先跳到 task 9 的 Picker 再回来人工测。

推荐策略：**跳过这步，等 task 9 把 Picker 加上再统一人工验证。**

- [ ] **Step 9: Commit**

```bash
git add mux0/Localization/Strings.xcstrings mux0/Sidebar/SidebarView.swift mux0/Sidebar/WorkspaceListView.swift mux0/Bridge/SidebarListBridge.swift mux0/ContentView.swift
git commit -m "feat(i18n): migrate sidebar strings to L10n"
```

---

## Task 8: 迁移 Tab 条 + SettingsSection.label + Settings shell

**Files:**
- Modify: `mux0/Localization/Strings.xcstrings`
- Modify: `mux0/Bridge/TabBridge.swift`
- Modify: `mux0/TabContent/TabBarView.swift`
- Modify: `mux0/TabContent/TabContentView.swift`
- Modify: `mux0/Settings/SettingsSection.swift`
- Modify: `mux0/Settings/SettingsView.swift`
- Modify: `mux0/Settings/SettingsTabBarView.swift`

- [ ] **Step 1: Catalog 补键**

| key | en | zh-Hans |
|-----|----|---------|
| `tab.newTab` | "New tab" | "新建 tab" |
| `settings.section.appearance` | "Appearance" | "外观" |
| `settings.section.font` | "Font" | "字体" |
| `settings.section.terminal` | "Terminal" | "终端" |
| `settings.section.shell` | "Shell" | "Shell" |
| `settings.close` | "Close settings" | "关闭设置" |
| `settings.footer.edit` | "Edit Config File…" | "编辑配置文件…" |
| `settings.footer.live` | "Changes apply live." | "修改即时生效。" |

- [ ] **Step 2: 改 `SettingsSection.swift`** — label 返回 `LocalizedStringResource`

```swift
import Foundation

enum SettingsSection: String, CaseIterable, Identifiable {
    case appearance
    case font
    case terminal
    case shell

    var id: String { rawValue }

    var label: LocalizedStringResource {                        // ← 改返回类型
        switch self {
        case .appearance: return L10n.Settings.sectionAppearance
        case .font:       return L10n.Settings.sectionFont
        case .terminal:   return L10n.Settings.sectionTerminal
        case .shell:      return L10n.Settings.sectionShell
        }
    }
}
```

- [ ] **Step 3: 改 `SettingsTabBarView.swift`** — pill 里 `Text(section.label)` 已直接吃 `LocalizedStringResource`，无须改；close 按钮 tooltip：

```swift
IconButton(theme: theme, help: String(localized: L10n.Settings.close), action: onClose) {   // ← 改
    Image(systemName: "xmark")
        // ...
}
```

- [ ] **Step 4: 改 `SettingsView.swift`** — footer

```swift
private var footer: some View {
    HStack {
        TextLinkButton(theme: theme, title: String(localized: L10n.Settings.footerEdit)) {  // ← 改
            settings.openInEditor()
        }
        Spacer()
        Text(L10n.Settings.footerLive)                                                      // ← 改
            .font(Font(DT.Font.small))
            .foregroundColor(Color(theme.textTertiary))
    }
    // ...
}
```

- [ ] **Step 5: 改 `TabBarView.swift`** — tooltip & 空态

```bash
grep -n 'stringValue\|toolTip\|placeholderString\|Text(\"' mux0/TabContent/TabBarView.swift
```

对找到的硬编码（"+"符号不需要；"New tab" tooltip 等需要）逐一替换。示例：

```swift
plusButton.toolTip = L10n.string("tab.newTab")   // ← 原为硬编码 "New tab" / nil
```

加 `refreshLocalizedStrings()`：

```swift
extension TabBarView {
    func refreshLocalizedStrings() {
        plusButton.toolTip = L10n.string("tab.newTab")
        // ... 对每个持有文案的 subview 重赋值
    }
}
```

- [ ] **Step 6: 改 `TabContentView.swift`** — 若 grep 找到静态文案同上处理；同时把 `languageTick` 透传给子 view（如果需要）。

```bash
grep -n 'stringValue\|toolTip\|Text(\"' mux0/TabContent/TabContentView.swift
```

- [ ] **Step 7: 改 `TabBridge.swift`** — 新增 `languageTick` 参数 + updateNSView 调 `refreshLocalizedStrings()`

模式同 SidebarListBridge（task 7 step 3）。

- [ ] **Step 8: `ContentView.swift` 向 TabBridge 透传 `languageTick`**

```swift
TabBridge(
    // ...
    showStatusIndicators: showStatusIndicators,
    languageTick: languageStore.tick                                // ← 新增
)
```

- [ ] **Step 9: Build + test**

```bash
xcodebuild build -project mux0.xcodeproj -scheme mux0 2>&1 | grep -E "(BUILD|error:)" | tail -5
xcodebuild test -project mux0.xcodeproj -scheme mux0Tests 2>&1 | grep -E "(Executed.*tests|\*\* TEST)" | tail -5
```
Expected: SUCCEEDED。

- [ ] **Step 10: Commit**

```bash
git add mux0/Localization/Strings.xcstrings mux0/Settings/SettingsSection.swift mux0/Settings/SettingsView.swift mux0/Settings/SettingsTabBarView.swift mux0/TabContent/TabBarView.swift mux0/TabContent/TabContentView.swift mux0/Bridge/TabBridge.swift mux0/ContentView.swift
git commit -m "feat(i18n): migrate tab bar + settings shell to L10n"
```

---

## Task 9: 迁移 Settings Section 字段 + 新增 Language Picker 行

**Files:**
- Modify: `mux0/Localization/Strings.xcstrings`
- Modify: `mux0/Localization/L10n.swift`
- Modify: `mux0/Settings/Sections/AppearanceSectionView.swift`
- Modify: `mux0/Settings/Sections/FontSectionView.swift`
- Modify: `mux0/Settings/Sections/TerminalSectionView.swift`
- Modify: `mux0/Settings/Sections/ShellSectionView.swift`
- Modify: `mux0/Settings/Components/SettingsResetRow.swift`
- Modify: `mux0/Settings/Components/FontPickerView.swift`

- [ ] **Step 1: `grep` 出所有 Settings Section / Component 里的硬编码文案**

```bash
grep -rn '\"[A-Z][A-Za-z ()…]*\"' mux0/Settings/Sections mux0/Settings/Components 2>/dev/null | grep -v 'import\|@Observable\|systemName\|String(localized\|UserDefaults'
```

列出所有需要迁移的 key-candidate。逐个命名（dotted namespace 风格：`settings.<section>.<field>`、`settings.font.default` 等）。

- [ ] **Step 2: 把所有新 key 加进 Catalog**

每个 key 写 en 原文 + 中文译文（根据现有英文语义翻译；所有**技术术语保留英文**，如 `Shell`、`PATH`、`bash`、`zsh` 不翻）。

**Language 相关 3 条必加：**

| key | en | zh-Hans |
|-----|----|---------|
| `settings.appearance.language` | "Language" | "语言" |
| `settings.language.system` | "System" | "跟随系统" |
| `settings.appearance.theme` | "Theme" | "主题" |
| `settings.appearance.opacity` | "Opacity" | "不透明度" |
| `settings.appearance.blur` | "Blur" | "模糊" |

（其他 `settings.font.*`、`settings.terminal.*`、`settings.shell.*`、`settings.reset.*` 按 step 1 grep 结果补齐。）

- [ ] **Step 3: 补 L10n 常量层**

根据 step 1 grep 结果，把每个新 key 加到 `L10n.swift` 对应 namespace（`L10n.Settings.Terminal.*` / `L10n.Settings.Shell.*` 等）。

示例（以 Terminal section 为假想，真实列表以 grep 输出为准）：

```swift
enum Terminal {
    static let cursorStyle      = LocalizedStringResource("settings.terminal.cursorStyle")
    static let cursorBlink      = LocalizedStringResource("settings.terminal.cursorBlink")
    // ...
}
```

- [ ] **Step 4: 把所有 Section / Component 里的硬编码字面量替换成 L10n 引用**

SwiftUI 中 `Text("Foo")` → `Text(L10n.Settings.xxx)`；label 形参 `title: "Foo"` → `title: String(localized: L10n.Settings.xxx)` 或把参数类型改成 `LocalizedStringResource`（按现有 component 签名选）。

`FontPickerView.swift` 具体：

```swift
Text("(default)").tag("")                    →  Text(L10n.Settings.fontDefault).tag("")
Text("Custom…").tag("__custom__")            →  Text(L10n.Settings.fontCustom).tag("__custom__")
```

`SettingsResetRow.swift`：

```swift
Text("All settings in this section will revert to their defaults. ...")
  →  Text(L10n.Settings.resetMessage)
```

- [ ] **Step 5: 在 `AppearanceSectionView.swift` 新增 Language Picker 行**

插到现有 Opacity / Blur 字段之后：

```swift
@Environment(LanguageStore.self) private var languageStore  // ← 加到 struct 顶部

// ... 现有 body 内，Opacity 行之后：

LabeledContent {
    Picker("", selection: Binding(
        get: { languageStore.preference },
        set: { languageStore.preference = $0 }
    )) {
        Text(L10n.Settings.languageSystem).tag(LanguageStore.Preference.system)
        // "中文（简体）" / "English" 永远以本语言名显示 —— 硬编码，不过 L10n
        Text("中文（简体）").tag(LanguageStore.Preference.zh)
        Text("English").tag(LanguageStore.Preference.en)
    }
    .pickerStyle(.menu)
    .labelsHidden()
} label: {
    Text(L10n.Settings.language)
        .font(Font(DT.Font.body))
        .foregroundColor(Color(theme.textPrimary))
}
```

格式需与同文件内其他 `LabeledContent` / `BoundPicker` 调用风格一致；若 Appearance 用的是项目自定义 `BoundPicker`，则用那个：

```swift
BoundPicker(
    label: String(localized: L10n.Settings.language),
    theme: theme,
    selection: Binding(
        get: { languageStore.preference },
        set: { languageStore.preference = $0 }
    )
) {
    Text(L10n.Settings.languageSystem).tag(LanguageStore.Preference.system)
    Text("中文（简体）").tag(LanguageStore.Preference.zh)
    Text("English").tag(LanguageStore.Preference.en)
}
```

阅读 `Settings/Components/BoundControls.swift` 确认准确签名后再写。

- [ ] **Step 6: `SettingsResetRow.swift`**（若 reset 会重置 Appearance 的所有字段）确保 Language 也在 reset 范围里，或保持"Language 不受 reset 影响"。

**推荐不把 Language 放入 Appearance reset** —— 因为 Language 是 UserDefaults 不是 ghostty config，`SettingsResetRow` 原本只 reset ghostty config 字段。保持现状即可，Language 的 reset 由单独机制处理（或不需要）。

- [ ] **Step 7: Build + test + 人工测试**

```bash
xcodebuild build -project mux0.xcodeproj -scheme mux0 2>&1 | grep -E "(BUILD|error:)" | tail -5
xcodebuild test -project mux0.xcodeproj -scheme mux0Tests 2>&1 | grep -E "(Executed.*tests|\*\* TEST)" | tail -5
```

然后运行 app（`xcodebuild -project mux0.xcodeproj -scheme mux0 build && open ...app`），在 Settings > Appearance 选 Language 试切 System/中文/English，**人工确认**：

1. Sidebar 标题、+ tooltip、设置 tooltip、删除 alert 都切换
2. Settings 面板各 section 名、字段 label、footer "Changes apply live." / "Edit Config File…" 都切换
3. Tab 条 "+" tooltip 切换
4. 选项回到 System 时，跟 `Locale.current` 匹配

如果菜单栏不跟着切，**记录现象**到 task 10 的 step 6（可能需要重建 Commands 或改用 `@Environment(\.locale)` 手动透传给 Commands）。

- [ ] **Step 8: Commit**

```bash
git add -A mux0/Localization mux0/Settings
git commit -m "feat(i18n): migrate settings sections + add Language picker row"
```

---

## Task 10: 迁移 App 级文案（GhosttyMissingView + Commands 菜单）

**Files:**
- Modify: `mux0/Localization/Strings.xcstrings`
- Modify: `mux0/mux0App.swift`

- [ ] **Step 1: Catalog 补键**

| key | en | zh-Hans |
|-----|----|---------|
| `app.ghostty.notFound.title` | "Ghostty not found" | "未找到 Ghostty" |
| `app.ghostty.notFound.detail` | "mux0 requires Ghostty to be installed.\nRun scripts/build-vendor.sh first, then relaunch." | "mux0 需要安装 Ghostty。请先运行 scripts/build-vendor.sh，然后重新启动。" |
| `menu.settings` | "Settings…" | "设置…" |
| `menu.editConfig` | "Edit Config File…" | "编辑配置文件…" |
| `menu.newWorkspace` | "New Workspace" | "新建 Workspace" |
| `menu.newTab` | "New Tab" | "新建 Tab" |
| `menu.closePane` | "Close Pane" | "关闭窗格" |
| `menu.splitVertical` | "Split Vertically (Left/Right)" | "垂直分割（左/右）" |
| `menu.splitHorizontal` | "Split Horizontally (Top/Bottom)" | "水平分割（上/下）" |
| `menu.focusNextPane` | "Focus Next Pane" | "聚焦下一窗格" |
| `menu.focusPrevPane` | "Focus Previous Pane" | "聚焦上一窗格" |
| `menu.selectNextTab` | "Select Next Tab" | "下一个 Tab" |
| `menu.selectPrevTab` | "Select Previous Tab" | "上一个 Tab" |
| `menu.selectTab %lld` | "Select Tab %lld" | "切换到第 %lld 个 Tab" |
| `menu.terminal` | "Terminal" | "终端" |
| `menu.copy` | "Copy" | "拷贝" |
| `menu.paste` | "Paste" | "粘贴" |
| `menu.selectAll` | "Select All" | "全选" |
| `menu.help` | "mux0 Help" | "mux0 帮助" |

- [ ] **Step 2: 改 `GhosttyMissingView`**

```swift
struct GhosttyMissingView: View {
    var body: some View {
        VStack(spacing: 16) {
            Image(systemName: "exclamationmark.triangle")
                .font(.system(size: 40))
                .foregroundColor(.orange)
            Text(L10n.App.ghosttyNotFoundTitle)                     // ← 改
                .font(.title2.bold())
            Text(L10n.App.ghosttyNotFoundDetail)                    // ← 改
                .multilineTextAlignment(.center)
                .foregroundColor(.secondary)
        }
        .padding(40)
        .frame(width: 400, height: 280)
    }
}
```

- [ ] **Step 3: 改 `Commands` 菜单项**

全部 `Button("...")` 里的字面量换成 `LocalizedStringResource`（SwiftUI `Button(_:action:)` 支持以 `LocalizedStringKey` 或 `LocalizedStringResource` 初始化）：

```swift
Button(L10n.Menu.settings) { post(.mux0OpenSettings) }
    .keyboardShortcut(",", modifiers: .command)

Button(L10n.Menu.editConfig) { post(.mux0EditConfigFile) }

// ... 同样改其他全部 Button(...)
```

对 `Select Tab %d` 动态 n：

```swift
ForEach(1...9, id: \.self) { idx in
    Button(L10n.Menu.selectTabN(idx)) { ... }
        .keyboardShortcut(KeyEquivalent(Character(String(idx))), modifiers: .command)
}
```

`CommandMenu("Terminal")` → `CommandMenu(String(localized: L10n.Menu.terminalMenu))`。

`Button("mux0 Help") {}.disabled(true)` → `Button(L10n.Menu.help) {}.disabled(true)`。

- [ ] **Step 4: Build + 人工验证菜单**

```bash
xcodebuild build -project mux0.xcodeproj -scheme mux0 2>&1 | grep -E "(BUILD|error:)" | tail -5
```

运行 app，切 Language 到中文，看菜单栏（File / Edit / Terminal）是否中文显示。

- [ ] **Step 5: 处理菜单栏不响应 locale 的降级**

**若 step 4 发现菜单栏文字不随 Language 切换**（只有 Commands 初次创建时的 locale 生效）：

降级方案：把 `var body: some Scene` 里依赖 `languageStore.tick` 作为 `@State` 驱动重新计算。SwiftUI 的 `@Observable` 访问 `languageStore.tick` 会登记观察；把 `tick` 的读取放在 `body` scope 内即可：

```swift
var body: some Scene {
    _ = languageStore.tick     // ← 显式引用以建立依赖
    return WindowGroup { ... }
        .windowStyle(.hiddenTitleBar)
        .commands { ... }
}
```

若仍不生效（SwiftUI Commands 有些场景不遵循 scene 级 env 更新），最终降级是用 `AppDelegate` 的 `applicationDidFinishLaunching` 监听 `languageStore` 变化后遍历 `NSApp.mainMenu` 重写每项 `title`。这条路**先不实现**，在 step 4 出现问题时再加。

- [ ] **Step 6: Commit**

```bash
git add mux0/Localization/Strings.xcstrings mux0/mux0App.swift
git commit -m "feat(i18n): migrate ghostty-not-found view + menu commands to L10n"
```

---

## Task 11: Smoke 测试 — 覆盖所有 key 在 zh/en bundle 下可查

**Files:**
- Create: `mux0Tests/L10nSmokeTests.swift`

- [ ] **Step 1: 写 smoke 测试**

枚举所有 key（直接从 `Strings.xcstrings` 里读出 `strings` object 的 keys 数组，或者手维护一个常量列表）。为可控性与测试清晰度，**手维护列表**：

```swift
import XCTest
@testable import mux0

final class L10nSmokeTests: XCTestCase {
    /// 所有 key 的硬编码列表。Catalog 增删 key 时同步维护。
    /// 违反 DRY 是故意的：让测试本身成为 key 清单的单点校验器。
    private let allKeys: [String] = [
        // Sidebar
        "sidebar.title",
        "sidebar.newWorkspace",
        "sidebar.settings",
        "sidebar.show",
        "sidebar.hide",
        "sidebar.deleteAlert.title",
        "sidebar.deleteAlert.message %@",
        "sidebar.deleteAlert.cancel",
        "sidebar.deleteAlert.confirm",
        // Tab
        "tab.newTab",
        // Settings shell
        "settings.section.appearance",
        "settings.section.font",
        "settings.section.terminal",
        "settings.section.shell",
        "settings.close",
        "settings.footer.edit",
        "settings.footer.live",
        // Settings fields — **在 task 8 之后根据 grep 实际发现的 key 追加到此列表**
        "settings.appearance.theme",
        "settings.appearance.opacity",
        "settings.appearance.blur",
        "settings.appearance.language",
        "settings.language.system",
        "settings.font.family",
        "settings.font.size",
        "settings.font.default",
        "settings.font.custom",
        "settings.reset.title",
        "settings.reset.message",
        "settings.reset.button",
        // ... (按 task 8/9 grep 结果补 terminal.* / shell.*)
        // App
        "app.ghostty.notFound.title",
        "app.ghostty.notFound.detail",
        // Menu
        "menu.settings",
        "menu.editConfig",
        "menu.newWorkspace",
        "menu.newTab",
        "menu.closePane",
        "menu.splitVertical",
        "menu.splitHorizontal",
        "menu.focusNextPane",
        "menu.focusPrevPane",
        "menu.selectNextTab",
        "menu.selectPrevTab",
        "menu.selectTab %lld",
        "menu.terminal",
        "menu.copy",
        "menu.paste",
        "menu.selectAll",
        "menu.help",
    ]

    private func bundle(for code: String) -> Bundle? {
        let parent = Bundle(for: LanguageStore.self)
        guard let path = parent.path(forResource: code, ofType: "lproj") else { return nil }
        return Bundle(path: path)
    }

    func testAllKeysResolveInEnBundle() throws {
        let b = try XCTUnwrap(bundle(for: "en"), "en.lproj missing")
        for key in allKeys {
            let v = b.localizedString(forKey: key, value: "__MISSING__", table: nil)
            XCTAssertNotEqual(v, "__MISSING__", "missing en translation for: \(key)")
            XCTAssertFalse(v.isEmpty, "empty en translation for: \(key)")
        }
    }

    func testAllKeysResolveInZhHansBundle() throws {
        let b = try XCTUnwrap(bundle(for: "zh-Hans"), "zh-Hans.lproj missing")
        for key in allKeys {
            let v = b.localizedString(forKey: key, value: "__MISSING__", table: nil)
            XCTAssertNotEqual(v, "__MISSING__", "missing zh translation for: \(key)")
            XCTAssertFalse(v.isEmpty, "empty zh translation for: \(key)")
        }
    }

    func testFormattedMessageSubstitutesWorkspaceName() {
        LanguageStore.shared.preference = .en
        let s = L10n.string("sidebar.deleteAlert.message %@", "my-ws")
        XCTAssertTrue(s.contains("my-ws"))
        XCTAssertFalse(s.contains("%@"), "'%@' not substituted: \(s)")
    }
}
```

- [ ] **Step 2: 跑测试**

```bash
xcodebuild test -project mux0.xcodeproj -scheme mux0Tests 2>&1 | grep -E "(Executed.*tests|L10nSmoke|\*\* TEST)" | tail -15
```

Expected：若任何 key 漏翻（在 zh 下返回 key 本身），这一 test 失败并精确报出 key 名。

- [ ] **Step 3: 修补漏翻译**

若测试报某 key 失败：
- 回到 Catalog 给该 key 补 zh 翻译
- 重跑 smoke 直到全绿

- [ ] **Step 4: Commit**

```bash
git add mux0Tests/L10nSmokeTests.swift mux0/Localization/Strings.xcstrings
git commit -m "test(i18n): smoke-test all L10n keys in en + zh-Hans bundles"
```

---

## Task 12: 文档同步 + drift check

**Files:**
- Modify: `CLAUDE.md`
- Modify: `AGENTS.md`（若存在同字段）
- Modify: `docs/architecture.md`
- Create: `docs/i18n.md`

- [ ] **Step 1: `docs/i18n.md` — 开发者指南**

```markdown
# Internationalization (i18n)

mux0 supports English and Simplified Chinese UI. Language preference lives in
`UserDefaults` (`mux0.language`, values: `system|zh|en`) and is exposed in
Settings > Appearance > Language.

## Architecture

- `mux0/Localization/LanguageStore.swift` — `@Observable` singleton.
- `mux0/Localization/L10n.swift` — `enum L10n { enum Sidebar { … } … }` constants.
  - SwiftUI: `Text(L10n.Sidebar.newWorkspace)`
  - AppKit:  `label.stringValue = L10n.string("sidebar.newWorkspace")`
- `mux0/Localization/Strings.xcstrings` — Xcode String Catalog (English source +
  Chinese translation).

SwiftUI views pick up `\.locale` from the environment; `mux0App` injects
`languageStore.locale` at the root. AppKit subclasses rebuild their `stringValue`
when `SidebarListBridge` / `TabBridge` detect `languageTick` change and call
their subview's `refreshLocalizedStrings()`.

## Adding a new string

1. Edit `mux0/Localization/Strings.xcstrings` in Xcode; add the key with English
   + zh-Hans values.
2. Add a constant to the appropriate `L10n.*` namespace in
   `mux0/Localization/L10n.swift`.
3. Reference `L10n.Namespace.key` at the call site.
4. Add the key to `mux0Tests/L10nSmokeTests.swift allKeys[]` so the smoke test
   catches missing translations.

## Adding a new language

1. In the Catalog, add a new localization column (e.g. `ja`).
2. Extend `LanguageStore.Preference` with the new case.
3. Extend `LanguageStore.locale` and `effectiveBundle` mapping.
4. Add a `Text(...)`/`Text("日本語")` option to the Language picker in
   `AppearanceSectionView`.
5. Smoke test will cover it once keys are translated.
```

- [ ] **Step 2: 更新 `CLAUDE.md`**

- Directory Structure 末尾加 `Localization/`：
```
├── Localization/
│   ├── LanguageStore.swift  — @Observable，语言偏好（UserDefaults）+ effectiveBundle + tick
│   ├── L10n.swift           — 常量命名空间（SwiftUI: LocalizedStringResource；AppKit: string(_:args:)）
│   └── Strings.xcstrings    — Xcode String Catalog（en source + zh-Hans translation）
```

- Common Tasks 加一行：

| 任务 | 相关文件 / 命令 |
|------|----------------|
| 新增文案 / 支持新语言 | `mux0/Localization/Strings.xcstrings`, `mux0/Localization/L10n.swift`, `mux0Tests/L10nSmokeTests.swift`, `docs/i18n.md` |

- Documentation Map 加一行：

| 主题 | 文件 |
|------|------|
| 国际化 (i18n) | `docs/i18n.md` |

- [ ] **Step 3: 更新 `AGENTS.md`**（如存在同字段；项目里应该和 CLAUDE.md 同步）

```bash
diff mux0/Documents/repos/mux0/CLAUDE.md AGENTS.md 2>/dev/null | head  # 对比有哪些字段差异
```

然后把和 CLAUDE.md 同等的改动 apply 到 AGENTS.md。

- [ ] **Step 4: 更新 `docs/architecture.md`** — 新增 Localization 小节

在架构章节最后添加：

```markdown
### Localization (i18n)

The `mux0/Localization/` module localizes all user-facing text:

- `LanguageStore` (`@Observable`, singleton) holds user preference
  (`.system | .zh | .en`), persists to UserDefaults, publishes `tick` for
  AppKit refresh.
- SwiftUI text uses `Text(L10n.Namespace.key)`; locale comes from
  `mux0App`'s `.environment(\.locale, languageStore.locale)` injection.
- AppKit subclasses (`TabBarView`, `WorkspaceListView`, etc.) compute
  strings via `L10n.string("key")` against `LanguageStore.shared.effectiveBundle`.
  When language changes, each `*Bridge` reads `languageStore.tick`,
  triggering `updateNSView` which calls `refreshLocalizedStrings()` on its
  subview to rebuild all label values.

See `docs/i18n.md` for the developer guide.
```

- [ ] **Step 5: 跑 drift check**

```bash
./scripts/check-doc-drift.sh
```
Expected：无 drift 报告（或仅 localization 目录被正确登记）。

- [ ] **Step 6: Commit**

```bash
git add CLAUDE.md AGENTS.md docs/architecture.md docs/i18n.md
git commit -m "docs(i18n): add i18n.md + update CLAUDE/AGENTS + architecture section"
```

---

## Self-Review（写完计划后的自查，无须用户介入）

### Spec coverage check

对应 spec 里每条需求，看是否有任务覆盖：

| Spec 需求 | Task |
|----------|------|
| Appearance 新增 Language 行 | Task 9 step 5 |
| System/中文/English 三选一 | Task 9 step 5 |
| Picker 选项文本策略（中/英选项永远本语言） | Task 9 step 5（硬编码 "中文（简体）" / "English"） |
| UserDefaults 持久化 | Task 2（`didSet` 写 UserDefaults） |
| 切换立即生效（SwiftUI） | Task 6（注入 `\.locale`）+ Task 7/8/9（migration） |
| 切换立即生效（AppKit） | `languageTick` 参数 → Task 7/8 |
| 全量覆盖：Sidebar / Tab / Settings / 菜单 / 错误页 | Task 7 / 8 / 9 / 10 |
| 测试 LanguageStore | Task 2 + Task 4 |
| Smoke 测试所有 key | Task 11 |
| 文档同步 | Task 12 |
| scope 列表加 i18n | Task 1 |
| 不加 sidebar footer 按钮 | 计划里**没有**触碰 SidebarView 的 footer 按钮 —— 只改它的 tooltip |

**Gap 检查**：spec §4 "AppKit 侧即时刷新"机制在 plan task 7 step 3-4 和 task 8 step 5-7 明确实现。Terminal/Shell Section 字段 key 列表在 task 9 step 1 的 grep 环节动态发现（不是 hardcoded placeholder，合理）。

### Placeholder scan

- ❌ → ✅ Task 9 step 1 依赖 grep 动态发现 key 列表 → 这不是 "TODO 以后填"，而是 "现场操作指令"，保留。
- ❌ → ✅ Task 7 step 4 的 WorkspaceListView 条件分支（有/无硬编码文案两条路径）均完整给出代码，无 TBD。
- ❌ → ✅ Task 10 step 5 的降级方案有触发条件（step 4 现象）+ 具体代码，不是空口承诺。

### Type consistency

- `LanguageStore.Preference`（`.system / .zh / .en`）在 task 2/4/6/9/11 一致。
- `effectiveBundle: Bundle` / `locale: Locale` / `tick: Int` 签名在 task 4 定义后贯穿一致。
- `L10n.string(_:_args:)` 在 task 5 定义后 task 7/8/10/11 用法一致。
- `refreshLocalizedStrings()` 在 task 7（SidebarListBridge/WorkspaceListView）与 task 8（TabBridge/TabBarView/TabContentView）命名一致。
- `languageTick: Int` 参数在 task 7/8 bridge 两侧一致。

无发现不一致。

---

## 执行前提醒

- 每个 task 结束**一定要跑** `xcodebuild test` 全绿再提交。
- 发现被跳过的硬编码文案（grep 没抓到、人工测试才发现）随时补到对应 task，Catalog + L10n + smoke 三处同时更新。
- `project.yml` 任何改动都要先征求用户确认（task 3 step 4 已显式标注）。
- 推送前不要直接 push master；PR 走 `agent/i18n-zh-en` 分支。

